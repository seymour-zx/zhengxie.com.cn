# 项目迭代卡摘要（changelog）

> 性质：L1 按需读（被 MEMORY/用户点名才读）。本文件是 2026-08-21/22/24 三天流水日志的**精炼迭代卡**；原日志 `2026-08-21.md`/`2026-08-22.md` 已提炼删除（回档于 `.workbuddy/backups/backup_logs_remove_20260824_1430/`），`2026-08-24.md` 已精简保留。
> 用途：快速回看"哪一轮做了什么决策/锁死什么"，不翻全量流水。详细实现见回档或 `2026-08-24.md`。

## 设计语言与暗色模式（已锁死）
- 暗色 = 近黑暖漆面黑 `#0D0C0E` + 暖炭灰 `#171519` + 亮金 `#E8CB84` + 象牙白 `#EDE8E0`，**禁止蓝紫**。（v6 第八轮）
- 默认明亮模式：仅当 `localStorage='dark'` 才暗色，不跟随系统。（v6 第八轮）
- 404 页 FOUC 仅本地 dark 才暗色，无全局 `referrer` meta。（第十五/十六轮）

## 链接属性规则（build.py 域名白名单，已锁死）
- 优先级短路：同域 `_self` → 同族 `noopener` → 营销 `sponsored…` → 评论 `ugc…` → 暴露 `nofollow noopener` + `referrerpolicy=origin` → 默认 `nofollow noopener noreferrer`。
- 全部由 `build.py` 的 `LINK_ATTR_PRESET` 域名白名单决定，增删只改 py 一处。（第九/十轮）
- 全站**不设**全局 `<meta name="referrer" no-referrer>`（仅卡片 `<img>` 保留 `referrerpolicy=no-referrer` 防防盗链）。（第九轮）

## 外链/内链/资源路径约定
- 内链 = 绝对域名 `https://zhengxie.com.cn/...`；资源 = 相对路径。
- 根页 `assets/...`、子页 `../../assets/...`（均指向根唯一真源，本地 file:// 也能加载）。（第十六轮子页改造）
- 换域名只改 `build.py` 的 `SITE_DOMAIN` 一处。（第四轮）

## 页面骨架 S1–S6（+可演进）
- S1 导航产品页（build 生成）/ S2 全站中枢页「网站全景」`/pages/overview/` / S3 说明信息页 / S4 合规页 / S5 功能入口 / S6 文章页。
- **骨架可演进**：优先归集已有骨架，确有利则新增 S7+；不私增编号。（第二十/二十一轮）
- 博客/日记一律走 S6，不塞 S3/about。（第二十一轮）

## 目录结构（已定稿）
- `pages/` 统辖 S2/S3/S4/S5 全部说明/中枢/合规/功能页（含 overview）；导航频道用 `directory/<name>/`（S1 实例）。
- `units/`、`test.html` 开发残留已清。（第二十三轮）

## 备案号策略
- GitHub Pages 托管，无 ICP 备案；备案号 HTML 注释占位不渲染，迁移国内服务器后取消注释即用。（第十二/十三轮）

## 专家转介纪律（硬边界）
- 5 类必须转专家：法律合规 / SEO 收录策略 / 视觉设计评审 / ICP 备案 / 广告收益。AI 仅做技术草稿，不下专业结论。（第十九轮 → 现 CONVENTIONS.md 第二节）

## 2026-08-24 当日关键动作

## 2026-08-25 当日关键动作
- **backup.md 全空间备份**（第 2 铁律）：90 文件逐字备份 + 来源注释（C2），仅排除 changelog/时间日志；随 memory/ 进 Git，用户接受 public 公开。
- **MEMORY.md 瘦身**：11,119 → ≤3,000 字符（现约 2,936），注入达标；最高无上铁律 v3（触发=注入失败时）+ 文件改动铁律"首次"定义（agent 首次进入含切换）+ 软链安全顺序归位跨设备策略。
- **规则归属修正**：备份通则（变更→重生成 backup.md + agent 名称）归 backup 铁律段；`__pycache__` 原因备忘降为非铁律。
- **待确认处置**：④ npc xlsx 备份已删（回档 backups/npc_xlsx_remove_20260825_1327/）；⑤ 备份策略取消（有 backup.md 兜底）；⑥ 仓库确认 **public**（公开与页面防暴露不冲突）。
- **待确认剩余**：②禁止指向边界（①5.6「删前回档+确认」已失效、③build 产物方案甲均已拍板）。- **身份软链重建**：C盘 4 身份文件 → D盘空间内软链，跨设备身份/记忆同步恢复。
- **SEO 结构化**：sitemap 补全至 26 条；全站 JSON-LD（WebSite/WebPage/CollectionPage/AboutPage/ContactPage）+ BreadcrumbList。
- **面包屑下移**：从页面顶部 → 底部广告位②上方（样式：12px、金色上边线、`/` 分隔、当前项红）。回档 `backup_breadcrumb_bottom_20260824_1151/`。
- **流程与执行纪律**：建立四阶段迭代环 + 同步强制规则（堵"约定≠同步"断层，补 add 漏跟踪日志）。
- **文件治理**：合并 `dev-process-plan.md` + `execution-rules.md` 进 `CONVENTIONS.md` 第五节「流程与执行」并删两独立 md；立 MEMORY 为首读锚点（三级读取 L0/L1/L2）；提炼本 changelog（原日志保留）。
- **待办（未展开）**：备份纪律沉淀（孤儿备份/命名/是否可删的判断标准）——用户明确"留着后期讨论，现在不讨论、不临时增 md"。
- **③ build 产物进 Git·方案甲拍板（2026-08-25）**：站点 build 产物（index.html 等）进 Git、随仓库同步；与 CONVENTIONS 5.2「站点源码 html 强制同步」自洽。`.gitignore` 注释定稿、无新增忽略项；backup.md 已据第 2 铁律重生成（files=92，方案甲注释逐字入备）。
- **① CONVENTIONS 5.6「删前回档+确认」失效（2026-08-25）**：回档职责归 backup.md 第2铁律（全空间备份），确认职责归文件改动铁律；5.6 仅保留「处置出口/禁止裸放 md」核心。backup.md 据第2铁律重生成（files=92）。
- **② 禁止指向边界结案 + README 公开暴露铁律违规发现（2026-08-25）**：README 不归禁止指向律（归公开暴露律全禁内部引用），SKILL 去重指针由铁律2 例外覆盖 → ② 结案；但验证现网 README.md 多处暴露 `.workbuddy/`/CONVENTIONS.md（L11/13/76/80/88/94/117/126/249 及 L204-210 目录树）违公开暴露律，新增 ③ 待清理拍板。MEMORY 一度 3039 字符超 3000 → 精简回 2959；backup.md 据第2铁律重生成（files=92）。
- **③ README 公开暴露铁律违规清理完成（2026-08-25）**：按用户拍板（直接删内部块 + 目录树去 `.workbuddy/` 与 `.build/`）执行。删 2 个内部 blockquote（跨设备权威源约定 L11、必读 SKILL.md L13）、3.1 专家转介纪律节（L84-88，并重编号 3.2→3.1）、6 处 SKILL.md SOP 指针（L76/80/94/117/126/249）、1 处裸 SKILL.md 引用（L99）；目录树去 `.build/`（L179-184）与 `.workbuddy/`（L193-199）两棵子树、修正 `├── assets/`→`└── assets/`。grep 验证 `.workbuddy`/`CONVENTIONS`/`SKILL.md`/`专家转介` 全清零。残留 `assets/.build/*.py` build 命令多处不属 `.workbuddy/` 违规，但 v5 曾刻意隐藏 `.build/` 名，列为 ④ 待拍板是否脱敏。backup.md 据第2铁律重生成（files=92）。
- **MEMORY.md 整理（2026-08-25 15:11）**：删冗余"2026-08-25 升级"（第2铁律标题）及 L50/L63 冗余日期戳，释放约 36 字符（2998→2962），注入上限 3000 安全。待确认仍仅 ④（README `.build/` 脱敏）。第一铁律延伸提案（"注入护栏"硬上限，待三确认）→ 用户否决，改 3000 软上限（见下）。
- **MEMORY 3000 软上限定稿（2026-08-25 15:20）**：否决"注入护栏"硬上限提案，改为放宽——MEMORY 写入不强制 ≤3000（可超）；超 3000 仅记为待办"自动注入将截断，需瘦身"，不拦截、不反复精算字符（降过程量/token 消耗）。第一铁律触发语义不变。
- **④ README 改写为手工写站指南（2026-08-25 15:35）**：用户拍板三项——①重写 README 为外人"手工写站"指南、②删除内部统计/广告真实 ID（百度统计双 id `2f4df505…`/`70e38224…`、GA4 `G-B880S4NQVK`、AdSense `ca-pub-6434243103158481`、slot `5952548493`/`4856101005` 全删）、③保留"手工视角"目录树（去 build 标注 + 去 json/xlsx 行）。全文删决策总览/状态标记/可接受清单/PENDING/BACKLOG 等内部运营机械，删全部 `python assets/.build/...`/`build_homeplus.py`/`collect_meta.py`/`check_links.py` 构建链路，删 `self_links.xlsx`/`self_meta.json`/`manifest.json` 数据源；xlsx 列定义改写为"在 HTML 里手写这些值"字段指引（第五节）。grep 验证 `.workbuddy`/`.build`/`python`/`xlsx`/`json/`/真实统计 ID 全 0。原 ④（`.build/` 脱敏）被本重写替代结案。backup.md 据第2铁律重生成（files=92）。
- **⑤ changelog=时间日志去重浓缩成文（2026-08-25 16:05）**：用户明确设定——changelog 记录变化范围=L12（同 backup 全空间受管文件）；时间日志=changelog 时间片段（⊂）；changelog=所有时间日志去重浓缩（∪dedup）。已写入 MEMORY「文件职责边界」段。backup.md 据第2铁律重生成。
- **⑥ 备份/审计指令改造（2026-08-25 16:59）**：用户拍板三项——①第2铁律通则改"受管文件变更→实时记时间日志；仅备份指令时重生成 backup.md"；②信息治理铁律3 改"时间日志兜底（实时层），changelog 为定期蒸馏层（备份指令时追加）"；③新增「备份/审计指令」定义段（备份=全空间快照+changelog 追加新内容；审计=三重验证：backup 完整性 / changelog 覆盖性 / 时间日志支撑性）。逻辑修正：原设想"changelog 蒸馏结果=backup.md"不成立（backup=内容态镜像，changelog=事件态变更史，不可相等），改为覆盖范围一致（均 L12）。本次 MEMORY 变更仅记时间日志、未重生成 backup.md——新铁律首次生效执行。
- **⑦ 时间日志生命周期确立（2026-08-25 17:20）**：用户拍板三项——1乙 不追溯历史（规则自 08-26 起执行，既有日志保持现状）；2乙 历史 backup 版本实体存 `backups/` 带日期副本，changelog 用指针不全文嵌入（去重指针合法）；3甲 changelog 滚动保留上一版 backup 指针作基线锚点。结构规则：最旧日志顶部=当时 backup 快照+当日记录，后续日志=仅变更记录；清理合并旧日志为归档文件（如 `2026-08-20-23.merged.md`），合并后蒸馏 changelog 须信息等价。已写入 MEMORY「时间日志生命周期」段。
- **⑧ 时间日志生命周期补充·首日志前置动作（2026-08-25 17:27）**：用户拍板 A——时间日志顶部只嵌指针（指向 `backups/` 日期实体副本），不全文嵌入（防日志膨胀）。MEMORY 补「新体系首日志前置动作（08-26 首日必做）」：① 先执行备份指令（生成含 25 号全部变更的新 backup.md）② 实体副本存 `backups/backup-20260826.md` ③ 建 26 号日志顶部嵌指针 ④ 其余正常记变更。待办区补「⏳ 08-26 首日必做」跨会话提醒。
- **⑨ 安全边界段删除（2026-08-25 17:34）**：用户拍板 A 整条删除 MEMORY「安全边界」段。判定：内容大部分被跨设备数据策略三件套覆盖（用户级技能=①、用户级记忆=②、身份软链=③）；唯一独立信息（workbuddy.db/mcp.json/受管 node·python 保持本地）为空间结构天然事实（均在 `~/.workbuddy/` 工作空间外）、非行为规则，按信息治理铁律①冗余即删。
- **⑩ .gitignore 同步记录铁律迁入 CONVENTIONS（2026-08-25 17:36）**：用户判定该段属成文约定（读者含用户）应归 CONVENTIONS 单一权威源，不占 MEMORY 注入名额。执行：MEMORY 删原段（L77-80）→ CONVENTIONS 5.2 后新增「5.2.1 .gitignore 同步记录铁律」，措辞同步新通则（受管文件增删改→实时记时间日志，备份指令时重生成 backup.md 比对）。MEMORY 再减约 200 字符。
- **⑪ MEMORY/CONVENTIONS 架构整理（2026-08-25 17:46）**：确立"MEMORY=纯步骤、CONVENTIONS=扩展版（含为什么）、双向可查"架构。MEMORY 重写为纯步骤版（删项目背景段、解释句移六章）；CONVENTIONS 追加「六、MEMORY.md 扩展解读」（6.1~6.13 逐条对应 MEMORY 章节，含背景/决策依据/演进理由），原一~五章被覆盖小节加"见 §6.x"指针，站点技术类保留。MEMORY 现 3362 字符（仍超 3000 软上限，待办挂起）。
- **⑫ CONVENTIONS 重构：MEMORY 章节前移 + 合并重复（2026-08-25 17:51）**：按用户指令整文件重写——原「六、MEMORY 扩展解读」前移为新「一」（1.1~1.13 实时对应 MEMORY 结构）；原「一、跨设备权威源约定」并入 1.7；原 5.2 同步强制 + 5.2.1 .gitignore + 5.5 冲突裁决并入 1.10；原 5.4 首读锚点并入 1.6；原 5.6 文档生命周期并入 1.13；保留二专家转介/三 JSON-LD/四面包屑/五流程（5.1 迭代 + 原 5.3 归档重编 5.2）。MEMORY 指针同步 §6→§1（三处），grep 验证零残留。
- **⑬ MEMORY 删信息治理铁律4（历史记录不入 MEMORY）（2026-08-25 17:56）**：用户判定信息治理铁律 4（5.6 失效+职责移交说明）为历史变更记录而非步骤，不应占 MEMORY 名额。归位：失效声明已在 CONVENTIONS §1.13 全文、"回档归 backup/确认归文件改动"已分别在两铁律自身、"5.6 失效"历史已在 changelog ① 与日志。MEMORY 信息治理铁律仅留 1-3（冗余即删/完整复制/时间日志兜底）。
- **⑭ 按"历史记录归位"逻辑整理三文件（2026-08-25 17:57）**：CONVENTIONS 第一章删 3 处纯历史记录（1.6 md 精简结果、1.10 .gitignore 变更展开句、1.11 README 清理事件句）——均已在 changelog/日志有完整记录；MEMORY 待办措辞更新（现约 3270）；时间日志确认 append-only 定位、历史归位正确。

## 已知遗留（BACKLOG）
- `pages/` 子页 assets 已改 `../../assets/` 引用；无未清残留。
- 营销/评论域名预设 `MARKETING = []` / `UGCCOMMENT = []` 待增删。（第十轮）
- 榜单（收录榜/访问榜）数据 PENDING，预留 S7 升级口。（第二十一轮）
- README.md 公开暴露铁律违规：③ 清理 + ④ 重写为手工写站指南，均已完成（见上方 ③/④ 条目），本 BACKLOG 出列。
