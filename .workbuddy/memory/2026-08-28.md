# 2026-08-28 工作日志

## build_homeplus.py：fav_key 回退去重 + dir_path 预留字段
- **fav_key 回退修复**（build_card，约 L659）：无链接时星标 key 由 `title or category or "card"` 改为 `(title or category or 'card')#row_seq`，用 row_seq 兜底去重，避免无链接同标题/同分类卡片收藏撞车。有链接仍用首个 URL。
  - 副作用：已收藏用户 localStorage 旧 key（纯标题/分类）下次打开会掉星，需重点（本地收藏本为临时性，已告知用户）。
- **新增 DIR_PATH 常量**（`DIR_PATH = "dir_path"`，置于 ROW_SEQ 之后）：预留占位，目前不参与渲染（面包屑/内链接入备用，类比 cat_id）。同步加入 `HEADER_NORMALIZE`（恒等映射），未进 `CORE_KEYS`（避免强制要求 xlsx 必有该列）。
- 同步更新：① 顶部列清单注释 `row_seq | dir_path | cat_id | …`；② 目录页列说明注释 `row_seq/dir_path/cat_name/…`。

## 三份 self_links.xlsx 插入空 dir_path 列
- 文件：root `assets/xlsx/self_links.xlsx`、`./directory/gov/assets/xlsx/self_links.xlsx`、`./directory/engine/assets/xlsx/self_links.xlsx`。
- 在 `row_seq` 列之后插入一列 `dir_path`（单元格留空占位），行数不变（65/59/67 数据行），现 31 列。
- 各备份：`*.xlsx.bak-20260828`（行数/表头已校验）。

## 验证
- `python assets/.build/build_homeplus.py` → exit 0；根页 65、engine 67、gov 59 张卡片，分类正常，无回归。
- 生成 HTML 核验：data-key 用 URL（当前全卡有链接，#回退路径未触发，属安全兜底）；DIR_PATH 常量与 HEADER_NORMALIZE 映射生效。

## ⏳ 待清理 / 待确认
- 备份堆积：`.bak-20260827`（上轮）+ `.bak-20260828`（本轮）共 6 个 xlsx 备份待用户确认后清理。
- Git 全停令下未提交；改动待用户本地自行提交（build_homeplus.py + 3 个 xlsx + 生成的 3 个 index.html）。

## 政协站点：命名规范 + 标签体系（用户拍板）
- **全称规范**：全国/省级/副省级/地级 名称本身唯一，写"××省政协""××市政协"即可；**县级及以下必须带上级**（区/县/县级市全国大量重名）→ "汕头市金平区政协""韶关市仁化县政协""苏州市昆山市政协"，副省级市的区照带（"广州市天河区政协"）。与公文"××市××区"写法一致。
- **标签双维度（数量不限）**：级别标签(国家级/省级/副省级/地级/县级) + 类型标签(全国/省/自治区/直辖市/地级市/自治州/地区/盟/县/县级市/市辖区/自治县/旗/林区/特区/兵团) + 组合标签(级别+政协、类型+政协，市辖区另加 区政协) + 细分标签(省会城市/计划单列市/民族自治区) + 自身名 + 上级链 + 地理大区。
- **副省级 15 市**：级别标 副省级、类型仍标 地级市（副省级市是地级市里级别更高的子类），两者并列不冲突；其中 10 省会城市、5 计划单列市加细分标签。
- **不列入地方政协序列**：乡级(镇/乡/街道，不设政协委)、香港/澳门(无本级政协)、台湾(按一个中国原则不单设地方政协委)。新疆兵团单列省级特殊。

## 产出：全国政協委员会全量名单
- 脚本取数 modood/Administrative-divisions-of-China 的 `dist/pca.json`（省→市→[区县]），转政协全称+标签，落盘 `.workbuddy/generated/all_cppcc.csv`。
- 统计：共 **3337 家** = 全国1 + 省级32(31省区市+兵团) + 副省级15 + 地级319 + 县级2970。剔除乡级 86 条（东莞/中山直筒子市下的街道镇、白沟新城、松山湖等园区，无政协委）。
- 类型分布：市辖区1103、县1299、县级市397、自治县117、旗52、自治州30、地区7、盟3、林区1(神农架)、特区1(六枝)、兵团1、自治区5、直辖市4、省22。
- 已知瑕疵处理：底表把个别功能区列为县级。已加「备注」列高精度标注 **121 条疑似功能区**（规则：含 开发区/管理区/示范区/工业园区/产业园区/片区/新区；排除 新城区、清新区 等真实市辖区，避免误报）。其中 110 条为开发区/管理区/园区类（几乎不单设政协委），11 条为 新区 类（滨海新区、浦东新区等国家级新区，多数有委，留作人工复核）。用户可据备注逐条核实后剔除或保留。
