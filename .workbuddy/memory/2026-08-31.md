# 2026-08-31 工作日志

## 性能优化（main.js + style.css）
- 诊断：小米浏览器卡顿根因 = 初始化同步强制重排 674 次 + 每次交互全量几何读取 + 吸顶栏 backdrop-filter 每帧全宽模糊重算。与卡片数量无直接关系。
- 改动（P0+P1 全做，已拍板）：
  - JS：预计算改惰性按需测量（初始化重排 674→0）；refreshScrollable 改视口感知增量（IntersectionObserver rootMargin 300px 补测，标记常驻）；highlightCard 加关键词签名脏检查；applyFilter 四阶段批处理；wheel 改 pointerover 缓存；3386 逐元素监听→document 事件委托（~10 个）；IntersectionObserver 统一 `window.` 前缀。
  - CSS：去 backdrop-filter（改 var(--bg) 不透明）；.card 加 content-visibility:auto + contain-intrinsic-size:auto 300px；9 处 transition:all→具体属性；.card:hover/.card__fav:hover 包进 @media (hover:hover) and (pointer:fine)。
- 实测（Playwright/Edge，视口1280×800）：初始化 longtask 784ms→185ms(↓76%)，最长 729→108ms(↓85%)；功能零回归；is-scrollable 整页滚动并集 328==优化前（零漏测）；content-visibility=auto。
- 回归闸门 qa-scroll-check.js 退出码 0（场景A/B 均过）。
- 回档：.workbuddy/backups/20260831-perf/{main.js,style.css}.bak
- 临时佐证文件（index-perf-old.html/main.old.js/style.old.css/perf-smoke.js/perf-cover.js）已清理，未进公开层。
- 未做：万张需虚拟滚动/分页（架构级，待实测数据再定）。
