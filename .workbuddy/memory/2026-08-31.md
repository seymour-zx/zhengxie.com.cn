# 2026-08-31

## 声明条（#consent-bar）Edge 无法关闭 —— 诊断（未改代码，待拍板）
- 方法：python http.server:8899 + playwright-core(channel msedge) 真实 Edge 152，11 组场景矩阵
- 结论：JS 逻辑无误。两条失败路径已复现
  - 根因①（高概率）：**Edge 缓存旧 style.css**（缺 `.official-banner[hidden]{display:none}`）→ hidden=true 但 display 仍 block = 视觉无反应
  - 根因②：`:has()`（需 Edge 105+）失效时 scroll-btns(z-index 90) 压住 banner(80)，关闭按钮被吃；窄屏几乎全被吃
- 已排除：内联脚本被拦、localStorage 被禁、file://、main.js 报错、单页问题
- 3 页一致（index / topics/gov / topics/search），CSS/JS 版本 hash 一致
- 修复候选：① JS 加 `bar.style.display='none'` 内联兜底；② ① + body.has-notice 类替代 :has()；③ ② + banner z-index 80→91
- 另案：**线上 zhengxie.com.cn HTML 落后本地**（线上无声明条、描述旧文案、CSS 用 preload 无版本号），但线上 CSS 已含 .official-banner 规则
- 报告：.workbuddy/reports/consent-bar-diag-20260831.md

## 实施：方案② 完整加固（已改完并验证）
- 4 文件改动：index.html / topics/gov / topics/search 内联脚本 + main.js 第13段（同一份逻辑，勿单边改）
  - 隐藏三重兜底：`hidden` + **内联 `style.display='none'`**（免疫旧 CSS 缺 [hidden] 规则）+ 撤避让
  - 避让不依赖 `:has()`：JS 按 `bar.offsetHeight` 实测写内联 `bottom`（桌面 +24 / 窄屏≤639 +16），resize 重算
  - main.js 新增「内联已关闭则保持关闭」分支，防重复显示
- style.css 新增 `body.has-notice .scroll-btns{76px}` + 移动端 84px（:has() 规则保留作无 JS 降级）
- 新增回归脚本 `assets/.build/qa-consent-check.js`（与 qa-scroll-check 并列，需 playwright-core + 本地 8899 服务）
- 验证：真实 Edge 7 组全 PASS（含旧 CSS 模拟，修复前 FAIL）；qa-scroll-check 退出码 0；node --check OK

## 追加排查：用户 Edge 点击+悬停全无效（00:25）
- probe-hit.js 对照实验（真实 Edge）：backdrop-filter / sticky-top pointer-events / 滚动前后 —— **hit-test 全部正常**，声明条内 25 点全命中 consent-bar/text，干净环境无法复现
- 结论：hover 纯 CSS 都失效 = 元素收不到鼠标事件，真凶在**用户 Edge 环境**（扩展注入透明层 / 脚本被拦截器阻止 / Edge 设置），代码层嫌疑已排除
- 新工具 `assets/.build/diag-index.html`（由 make-diag-page.py 生成）：用户 Edge 打开即自动扫描——声明条/关闭按钮/滚动按钮矩形内采样找覆盖者 + 收集 JS/资源错误 + iframe 清单，一键复制 JSON
- 坑：诊断页在 assets/.build/ 子目录，必须 `"assets/` → `"../../assets/` 否则资源 404 整页无样式；采样步长要覆盖 bottom:0 的 fixed 条（776-800 区间）
- 最快判定：InPrivate（Ctrl+Shift+N，扩展默认禁用）打开 8899 首页，正常=扩展凶手

## 工具沉淀
- 浏览器实测链路已通：`C:\Users\seymo\.workbuddy\binaries\node\workspace` 装 playwright-core，脚本 test-consent*.js；
  起服务器用 `python -m http.server 8899 --bind 127.0.0.1`；`channel:'msedge'` 调真实 Edge（本机 Edg/152）
- 模拟旧 CSS 缓存的正确姿势：`page.route` 拦截 style.css，正则剔除补丁规则后 fulfill
  （不要 addStyleTag + !important 覆盖，会掩盖真实层叠优先级，导致误判）

## P-C README 漂移修复（00:40，造物主拍板「按方案改」）✅
- 起因：MEMORY.md 路线行「P-C 待拍板」过时（实际 23:04 已收口）→ 已更正为「P-A/B/C 全收口」
- 漂移实锤：README 写于 23:04，当晚声明条连改三轮（30 天有效期 23:25 / 空间分离 23:39 / 独立内联 23:47）未同步
- 修复 5 处：L104/154/165/227「知道了」→「×」+ 补 30 天有效期；L152「z-index 必须高于声明条」→ 空间分离为主 + z-index 双保险
- 契约 06 校准清单 8 项全过（零内部引用 / 纯手工流 / 定位 / 目录树 / 章节 11 章 / 239 行）
- 契约 06 变更记录追加漂移回改实例 + 教训：功能连改多轮后必回查 README
- **下一步 = 第 10 步取代上线**：线上 HTML 落后本地（无声明条/无版本戳/旧文案），待造物主 push + 删壳

## 第 10 步发布前闸门（00:49）✅ 全部通过
- 坐标泄漏扫描（公开层）：3 命中 = topics/search 的 github.com/search 引擎入口 + README 部署模板 `<用户名>.github.io` 占位符，**零真泄漏** ✅
- verify_site.py：红线 0 / 严重 3（8-30 已拍板继承豁免）✅
- qa-scroll-check.js：退出码 0 ✅
- qa-consent-check.js（真实 Edge 7 组）：全部通过 ✅
- git status：仅 README + .workbuddy 治理/日志未提交；站点 HTML/CSS/JS 昨晚修复已入仓 → **本地可发布**
- 交棒造物主：git commit/push 发布 + 线上验证 + 旧世界删壳（agent 不碰 git）

## 代码审查（取代上线后自审，鹏城信息AI专家流程）✅ 报告落盘
- 报告：`.workbuddy/overviews/2026-08-31-代码审查报告.md`
- 结果：P0=0，P1=0（初判两项经每日日志核对已消解），P2=5，P3=1。
- 关键取证：
  - 坐标红线扫描 `*.html` 全量：无真实 github/seymo 坐标；README:195 占位符 = 发布闸门已裁决零真泄漏。
  - `qa-scroll-check.js` 实跑退出码 0（场景A/B 均 scrollTo 4 次、is-active 20 次）。
  - 结构契约全过：分类行/logo双文字/fav☆/卡片五形态+271空行占位/grid-break(t1→t2单处)/footer sun-moon/JSON-LD/**topics/search 精确 21 data-engine**。
  - 首页 search_box=None（读 pages.unified.xlsx 确认）→ 首页无 hero 搜索，非 bug 是配置(P2-2)。
- 复审修正：初判 P1-1(README)/P1-2(声明条闸门) 经日志核对已裁决/已PASS，降级为 P2-4/P2-5，不虚报 P1。
- P2 项：Claw/ 空目录待删；首页搜索意图拍板；about 境外托管+未备案(已知 staging)；README 占位符重申；声明条双闸门固化进发布流程。
- P3：canonical 尾斜杠不一致（首页无/，sitemap 有/）。

## 审查决议执行（01:19，造物主拍板）✅
- P2-1 删除 Claw/：真实文件系统已确认消失（沙箱 overlay 副本，删除时即 No such file）。
- P2-2 首页搜索：造物主「首页不需要」→ 维持 search_box=None，不改。
- P2-3 about 境外托管：造物主「可以」→ 接受为已知 staging 限制，待迁移境内+备案。
- P2-4 README 占位符：造物主「可以」→ 维持（发布闸门已裁决零真泄漏）。
- P2-5 双闸门固化：造物主「改回 main.js」→ 理解为保持双 QA 闸门锚定 main.js（既有 qa-*.js 测 main.js 行为），不另立发布流程文档；无代码改动。若造物主另有所指需指正。
- P3-1 canonical 尾斜杠：造物主问通用标准 → **标准=带尾斜杠**（全站 12 子页均带/、sitemap 首页带/，唯首页裸域名孤例）。已修 index.html 四处裸域名（canonical:14 / og:url:18 / JSON-LD url:36 / SearchAction target:36）统一加/，对齐 sitemap。验证 grep 无斜杠裸域名零命中。
- 收口状态：P0=0/P1=0；P2 全收口（P2-5 为理解性收口待确认）；唯一未固化＝双闸门依赖 playwright-core（受管 node 未含），换环境需重装。

## P3 根因修正 + 次生回归（01:24）⚠️
- 根因：`build_homeplus.py:151` `canonical = SITE_DOMAIN + ("" if dp=="/" else "/"+dp+"/")` — 首页 dp=="/" 取""→无斜杠；子页走 else→带斜杠。首页 canonical/og:url/JSON-LD 无斜杠的真源在此，直接改 index.html 会被下次 build 覆盖。
- 修复：首页分支改输出 "/"，一处修好四处（canonical/og:url/JSON-LD url/SearchAction target），对齐 sitemap+12 子页。
- 次生回归：重跑 build 发现 `NOTICE_SCRIPT` 仍是加固前旧版——此前声明条 30天+liftScrollBtns/hideBar/closeBar 加固是**直接手改 HTML 产物**做的，未回写 .py 真源 → 重跑把 index/gov/search 三页加固全回退成旧简单脚本。
- 修复：只读 `git show HEAD` 取回加固内联脚本（不改 git 状态），回写 `NOTICE_SCRIPT`，重跑。验证三页加固恢复、canonical 斜杠、版本戳为当前 hash。
- 教训（建议沉淀铁律）：改声明条/滚动等核心逻辑必须回写 build_homeplus.py 真源常量，不能只改产物，否则 rebuild 静默回退——与「改 main.js 必跑 qa-scroll-check / 改声明条必跑 qa-consent-check」同源：改源不改成品。

## 声明条 JS 收敛单一真源（01:31）✅
- 造物主指出声明条 JS 两份并存（HTML 内联 NOTICE_SCRIPT + main.js IIFE），要求只留 main.js。
- 取证：main.js:1001 已有完整强化版 IIFE（原定位「Edge 拦内联时由 main.js 接管」备用）；build_homeplus.py NOTICE_SCRIPT(L76-110)+{NOTICE_SCRIPT}(L742) 内联三页（原定位「主用+防 main.js 单点脆弱」）。两份逻辑逐行相同=冗余+内联 CSP 隐患。
- 执行：删 NOTICE_SCRIPT 常量+注入点（三页内联根除）；保留 main.js IIFE 为唯一真源，注释改「单一真源」；**未擅移 IIFE 位置**（暂留 L1001）。
- 验证：三页 grep zx_notice_closed=0、function liftScrollBtns=0；main.js 仍含（2行）；topics/search data-engine=21；三页 id=consent-bar 标记在；node --check OK；qa-scroll-check 退出码0（A/B 场景 scrollTo 4 / is-active 20）；main.js?v 刷新 c0b68874。
- ⚠️ 待拍板权衡：收敛后声明条完全依赖 main.js。main.js 是单大 IIFE，声明条 IIFE 在 L1001；若 L19–L1001 间抛错→整段中止→声明条不初始化（新访客关闭条卡死）。这正是当初内联要防的风险。
  - 加固建议：把声明条 IIFE 上移至 main.js 大 IIFE 顶部（'use strict' 后），自包含可安全移动；是否上移请造物主拍板。

## 声明条 IIFE 上移加固（01:36）✅
- 造物主拍板「上移」。声明条 IIFE 从 main.js 大 IIFE 内 L1001 → **L27**（紧贴 'use strict' 之后），全文件仅此一份。
- 效果：main.js 后续功能代码抛错也不连累声明条初始化——单一真源 + 原内联「不连累」保护兼得。
- 验证：node --check OK；qa-scroll-check 退出码0（A/B scrollTo 4 / is-active 20）；重建刷新 main.js?v=17795562；三页无内联声明条、21 引擎、声明条标记完好。
- 报告第八节「权衡点」已改记「上移加固已执行」。
