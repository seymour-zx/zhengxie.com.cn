# 2026-08-25 工作日志

## 会话开场
- 延续 2026-08-24 会话（用户已 git 同步、换设备继续）。MEMORY.md 现 21.2KB（已超注入限额，但顶部最高无上铁律必被注入）。
- memory/ 现状核查：3 类文件制保持完好（MEMORY.md / changelog.md / 2026-08-24.md），本日志为今日新建（第三种类型：时间命名日志）。

## 备份方案咨询（2026-08-25 11:09 用户问"备份全空间用一个md怎么处理"）
- **用户假设**：用一个 md 备份空间内所有文件内容 + 详细注释代码来源。
- **agent 裁决（基于已立铁律）**：不会用已有 4 类 md（MEMORY/CONVENTIONS/changelog/日志各有唯一职责），也不会新建到 memory/（3 类文件制）。
- **两条路**：
  - 方案 A（推荐）：本地私有备份 `.workbuddy/backups/backup_all_YYYYMMDD_HHMM.md` + MANIFEST，不进 Git、不公开（防公开暴露铁律：该备份含 py 配方/数据源/.workbuddy 内容 = 整仓复制件，绝不能进仓库公开路径）。
  - 方案 B：跨设备可读备份 → 须先过两关：①仓库确认 private（待确认 6）；②内容去敏（剥离配方，退化为"站点代码导读"放 `.workbuddy/docs/site-code-guide.md`）。
- **用户未拍板，待用户选择 A/B**。

## 对话保存载体确认（2026-08-25 11:09 用户问"对话翻译成agent语言存哪个md"）
- **用户问题**：所有对话翻译成 agent 语言保存，用新 md 还是已有 md？
- **agent 裁决**：**不新增 md 类型**，归入「时间命名日志」= `memory/2026-08-25.md`（本文件）。依据：3 类文件制（对话属第三种）+ 对话翻译铁律（当日事件→当日日志）+ 信息治理铁律（只落翻译后要点，不落全量原文）。
- **执行**：创建本日志并写入本会话要点（即本条）。

## 备份方案 C 提案 + 冲突识别（2026-08-25 11:14 用户选 C 方案）
- **用户方案 C**：新建 `backup.md` 放 `.workbuddy/memory/`；备份范围 = `.workbuddy/memory/` **之外**的所有空间内文件（不含 memory/ 内部，避免自引用）；内容 = 完整拷贝每个文件 + **逐字符解释来源**（该字符怎么来的/为什么是这个字符）。用户指令"记录为铁律"。
- **agent 识别 3 个冲突（已向用户指出，待裁决）**：
  1. **3 类文件制冲突**：memory/ 只允许 MEMORY/changelog/时间日志 3 类（用户 8-24 17:43 确认），backup.md 为第 4 类，需用户明确修改制度。
  2. **公开暴露铁律冲突**：memory/ 是强制同步进 Git 的目录；backup.md 含全站源码+配方注释=整仓复制件；若仓库 public，随 git 同步即公开全部——比 README 暴露更严重。
  3. **规模现实**：逐字符注释会使 backup.md 膨胀至数十 MB（仅 index.html 就 160KB），不可用；且注释本质=py 配方（正是要隐藏的）。
- **状态**：已记录用户指令，待用户对三冲突拍板后落盘执行。
- ⚠️ Git 全停令：仅写文件，未 add 未 commit。

## backup.md 生成执行完成（2026-08-25 11:18 用户裁决 C2 后执行）
- **用户最终裁决**：C2（逐字符完整备份）+ 接受 public 仓库风险 + 授权 memory/ 扩展 4 类 md + MEMORY.md 也复制（仅排除 changelog.md 与时间日志）。
- **铁律落档**：MEMORY.md 新增「backup.md 全空间备份铁律」段（文件位置/备份范围/内容要求/风险已知悉/执行纪律）。
- **生成执行**：Python 脚本遍历空间 90 个文件生成 `memory/backup.md`（1.42MB）：
  - 文本文件（69 个）：完整逐字符拷贝 + 来源注释（每个文件说明怎么来的/为什么）；
  - xlsx 数据源（17 个）：openpyxl 转文本表格忠实呈现；
  - webp/ico（4 个）：二进制无法文本化，登记路径/大小/用途。
- **踩坑修复**：首版脚本 `.git` 前缀判断误伤 `.gitignore`（89/90）→ 改为精确 `.git` 目录匹配后重跑 = 90/90 ✅；changelog/时间日志/backup 自身均无独立条目 ✅。
- **临时脚本清理**：gen_backup.py 放 backups/ 临时区，用后已删。
- **memory/ 现为 4 类**：MEMORY.md / changelog.md / 时间日志×2 / backup.md。
- ⚠️ **公开暴露提醒**：backup.md = 整仓复制件，随 memory/ 强制同步进 Git；用户已明确接受 public 仓库下全量公开（铁律已记）。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY.md 瘦身启动：gitignore 快照迁出（2026-08-25 11:30 用户指令）
- **背景**：MEMORY.md 现 22.4KB，远超注入限额（目标 ≤3,000 字符 ≈ 3KB），用户要求瘦身确保自动注入不中断。
- **已查证限额**（多源交叉验证）：项目级 MEMORY.md 注入目标 ≈ **3,000 字符**；写入限额 3,000 字符/会话（用户级 4,000）。现文件 22KB ≈ 限制 7.5 倍。
- **用户推理（采纳）**：backup.md 已含 .gitignore 全文 + 来源注释（备份铁律保证）→ MEMORY.md 删除 .gitignore 快照全文，防冗余/防两处漂移。
- **执行**：MEMORY.md「.gitignore 基准快照」段删除，铁律 2 修订为"以 backup.md 为权威载体，变更后重新生成 backup.md 比对"；保留「变更记录」（两条：8-24 删 assets 行、8-25 快照迁出）。节省 ~730 字符。
- **待续**：其余段落压缩至 ≤3,000 字符（当前约 10,400 字符，需再削 ~70%）。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY.md 瘦身完成（2026-08-25 11:31 执行完毕）
- **结果**：11,119 字符 → **2,969 字符**（≤3,000 达标），字节 22,439 → 5,938，削减 73%。
- **方法**：16 个段落全部保留核心规则，删除背景叙述/冗余解释（正文完整版在 CONVENTIONS 5.2/5.6 + backup.md 权威载体）；最高无上铁律保持文件最顶部第一位（注入截断只截尾部，必被注入）。
- **关键改动**：Git 提交/同步纪律合并为一段；公开暴露铁律压至 5 行；gitignore 快照全文迁至 backup.md（仅留变更记录）。
- **回档**：旧版备份于 `.workbuddy/backups/MEMORY.md.pre-slim-20260825_1130`（21.6KB，含删前完整版）。
- **验证**：2,969 ≤ 3,000 ✅；段落完整性 16 段 ✅；最高无上铁律位置保障 ✅。
- ⚠️ 提示：backup.md 内嵌的 MEMORY.md 副本仍是旧版（瘦身前 22KB 版）——按备份铁律应重新生成 backup.md 保持权威载体一致，待用户确认后执行。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY.md 段落融合（2026-08-25 11:47 用户指出重复）
- **用户发现**：「新设备/新会话开场必做」段与「跨设备数据策略」第 3 条重复了"C:→D: 软链接"机制（信息治理铁律 1：冗余即干扰）。
- **执行**：删独立「新设备/新会话开场必做」段，动作并入跨设备策略第 3 条（"开场必做：每次会话第一条动作检查 4 身份软链接"）。段落 16→14，字符 2,969→2,907。
- **⚠️ 踩坑记录**：首次 Edit 的 old_string 范围误吞「项目背景」段（该段在待融合两段中间）→ 立即恢复。教训：多段融合时 old_string 应精确到相邻边界，或分两次 Edit。
- ⚠️ Git 全停令：未 add 未 commit。

## 最高无上铁律修改意向：第 1 次授权（2026-08-25 12:04）
- **用户提出修改**（触发条件 + 流程简化）：
  1. 触发条件改为"**注入失败时才发生**"（MEMORY.md 完整注入时无需暂停确认——agent 已完整学习）；
  2. 流程简化：删除"是否授权学习空间内所有文件"环节，改为 ① 问是否学习 MEMORY.md 全文 → ② 同意→阅读→反馈→问是否继续被暂停的初始任务。
- **铁律修改门槛**：修改最高无上铁律须经用户**三次明确同意授权**，未满三次一律拒绝。本次 = **第 1 次**，未落盘，待第 2、3 次确认后执行。
- ⚠️ Git 全停令：未 add 未 commit。

## 最高无上铁律 v2 生效（2026-08-25 12:07 用户三次确认"确认，确认，确认"）
- **修改门槛满足**：用户连续三次明确确认（第 2+3 次于 12:07 一次给出），达到"三次明确同意授权"，落盘执行。
- **v2 内容**：
  1. **触发条件**：改为"仅当 MEMORY.md 自动注入失败（被截断）时"才触发确认制；注入完整时 agent 已完整学习，无需暂停确认。
  2. **流程**：删除"是否授权学习空间内所有文件"环节——① 问是否学习 MEMORY.md 全文 → ② 同意→阅读→反馈→问是否继续初始任务；③ 不同意→直接继续。
  3. 保留：修改门槛（三次授权）+ 位置保障（文件最顶部）。
- **验证**：字符 2,907→2,908（+1，仍 ≤3,000 达标）；最高无上铁律仍在第 3 行顶部。
- ⚠️ Git 全停令：未 add 未 commit。

## 最高无上铁律 v2 最终排版落盘（2026-08-25 12:11 用户给出精确版 + "授权，确认，确认，确认"）
- **用户提供最终排版**：触发=注入失败时才触发；流程=两问精简版；修改门槛=三次授权（去掉"v2 生效"标注）；位置保障=一句。
- **执行**：按用户精确文本重写最高无上铁律段（第 3-7 行），字符 2,908→2,864（≤3,000 达标）。
- **v2 终版要点**：触发条件从"任何首次进入"→"仅注入失败时"；流程删除"是否授权学习空间内所有文件"环节。
- ⚠️ Git 全停令：未 add 未 commit。

## 规则归属修正：备份通则归位（2026-08-25 12:21 用户指出）
- **用户指出**："任何增删改→重新生成 backup.md 并比对一致"是 backup.md 备份**所有文件**的通则，不是 .gitignore 专属——此前误写在 .gitignore 段，造成"只针对 .gitignore"的错误暗示。
- **修正**：通则（变更→重生成 backup.md 比对一致 + 变更记录注明 agent 名称 + 全文权威载体不重复快照）归入「backup.md 全空间备份铁律」段；.gitignore 段只留专属规则（__pycache__/__pyc 永不进 Git + 变更记录 + 指向通则）。
- **验证**：字符 2,864→2,930（≤3,000 达标）；两段结构干净。
- ⚠️ Git 全停令：未 add 未 commit。

## 规则归属二次修正（2026-08-25 12:25 用户指出）
- **用户两点**：
  1. "变更记录须注明 agent 名称"也是 backup.md 所有文件备份的标准——已确认：此条已在 backup 铁律段通则中（上一轮归位），.gitignore 段未重复。
  2. "`__pycache__/`、`*.pyc` 永不进 Git"没有资格成为铁律——只是备忘不进 git 的原因，真正规则在 .gitignore 文件本体（backup.md 已含全文）。
- **修正**：.gitignore 段删除"永不进 Git"伪铁律行，改为一句"注：原因备忘见 backup.md 中 .gitignore 条目来源注释（非铁律）"。
- **验证**：字符 2,930→2,982（≤3,000 达标）。
- ⚠️ 一致性提示：backup.md 中 .gitignore 条目来源注释尚未包含"__pycache__ 原因"备忘——待确认 ⑦（重新生成 backup.md）时一并更新。
- ⚠️ Git 全停令：未 add 未 commit。

## backup.md 铁律升级为第 2 铁律（2026-08-25 12:30 用户指令）
- **用户两点**：① 确认 backup.md 不含 .git 目录（实测 0 命中 ✅，但含 .gitignore）；② backup.md 全空间备份铁律**升级为第 2 铁律**（紧随最高无上铁律）。
- **执行**：backup 铁律段从"安全边界"后移到最高无上铁律之后（第 2 位），标题改「第 2 铁律，2026-08-25 升级」；范围明示"不含 `.git/` 目录"。
- **验证**：段落顺序 最高无上→backup→首读锚点 ✅；backup 段唯一（宽松匹配=1）✅；字符 2,982（≤3,000）✅。
- ⚠️ Git 全停令：未 add 未 commit。

## 文件改动铁律"首次"定义拍板 + 全文压缩至 3,000 达标（2026-08-25 12:33-12:45）
- **用户拍板方案 A（三次确认）**："首次"定义 = ① 注入完整 → 会话开场确认（最高无上铁律"同意学习"）即视为该会话首次授权，后续修改不再逐次确认；② 注入失败 → 走最高无上铁律确认流程后视为已授权。删除类动作不受"首次"覆盖，每次删除前仍须单独确认。
- **归属修正**："复制→删原→建链接→失败回滚"从文件改动铁律移出 → 归入跨设备数据策略第 3 条（身份软链专用安全顺序）。
- **压缩过程**：因新增定义使文件超限，多轮压缩（合并段、删冗余措辞、微调标点）最终 **3,000 字符整 ≤3,000 达标**；14 段完整。
- **⚠️ 踩坑记录**：压缩过程中多次 no-op 编辑误删段落间空行（格式损坏）→ 已修复恢复；"每条会话"误改已撤回为"每次会话"。教训：减字符应一次性替换整段，避免逐行微调。
- ⚠️ Git 全停令：未 add 未 commit。

## 待确认清单（承接 8-24，6 项，时刻提醒）
1. 信息治理铁律4：CONVENTIONS 5.6"删前必回档+用户确认"是否被"不怕错删"放宽。
2. 信息治理铁律2 边界：禁止指向是否覆盖 README/SKILL 去重指针。
3. build 产物（index.html 等）进 Git 策略：方案甲（进Git）vs 方案乙（本地build重建）。
4. `directory/gov/assets/xlsx/.workbuddy_backup_npc_20260824062310.xlsx` 是否冗余可删。
5. 备份保留策略（清理周期/保留个数）——用户"以后补"。
6. 🔴 GitHub 仓库可见性：域名层 .workbuddy/ 实测 404，但若仓库 public 则 .workbuddy/ 全部内容仓库页可见，防暴露前提不成立，须处置。
- 本会话新增待用户拍板：备份方案 C 的三个冲突（3 类文件制扩展、公开暴露风险、逐字符注释规模）——用户已选 C，待裁决冲突后执行。
- ⚠️ Git 全停令：仅写文件，未 add 未 commit。

## 待确认 ④⑤⑥ 拍板执行（2026-08-25 13:27 用户指令）
- **④ npc xlsx 删除**：用户确认删除。已回档 `.workbuddy/backups/npc_xlsx_remove_20260825_1327/`（文件 9,049B + MANIFEST 297B），原文件已删。注：首次执行 cp 报"不存在"（目录未建竞态），二次核查确认回档与删除均已成功——不纠结。
- **⑤ 备份保留策略取消**：用户判定"不用了，有 backup.md 了"——backup.md（第 2 铁律）已承担全空间备份职责，backups/ 保留策略不再需要。从待确认移除。
- **⑥ 仓库可见性确认 public**：用户确认 GitHub 仓库为 **public**，且判定"公开仓库"与"防止仓库地址在页面中暴露"**不冲突**（公开是仓库属性，页面防护照常执行）。已写入 MEMORY.md 公开暴露铁律段。
- **MEMORY.md 更新**：待确认清单 6→3 项（剩 ①5.6 放宽 ②禁止指向边界 ③build 产物进 Git）；公开暴露铁律段补仓库可见性结论；字符 2,859→2,936（≤3,000 达标）。
- **changelog 更新**：新增「2026-08-25 当日关键动作」段（backup 铁律/瘦身/规则归属/待确认处置/剩余项）。
- ⚠️ Git 全停令：未 add 未 commit。

## ③ build 产物进 Git 拍板·方案甲 + backup.md 重生成（2026-08-25 13:30）
- **③ 拍板**：用户选"方案甲（产物进 Git，随仓库同步）"。站点 build 产物（index.html、directory/<name>/index.html 由 build_homeplus.py 生成）进 Git、随仓库同步。
- **落地**：
  1. `.gitignore`：删除"歧义待定"注释，改定稿注释"同步策略（2026-08-25 拍板·方案甲）：产物进 Git、随仓库同步。依据 CONVENTIONS 5.2「站点源码 html」属强制同步文件"（不新增忽略项，产物仍被追踪）。
  2. `MEMORY.md` 待确认 3→2 项（删 ③），现：①5.6 放宽 ②禁止指向边界。字符 2,936→2,925（≤3,000 达标）。
  3. 决策锚点：CONVENTIONS 5.2 早已将"站点源码 html"列为强制同步文件，本次拍板与之自洽，无新增冗余。
- **第 2 铁律执行**：受管文件（MEMORY.md/.gitignore）变更 → 重生成 `memory/backup.md`（方案 C2，agent=ProductManagementExpert，files=92）。比对：`.gitignore` 段含新方案甲注释逐字一致；`MEMORY.md` 段待确认已无 ③ 逐字一致。排除正确（changelog/时间日志/backup 自身/.git 均不在索引）。
- **踩坑**：首版生成脚本把"2026-08-25.md"类时间日志误判可备份（违反"仅排除时间日志"），修正正则后 files 95→92；脚本置于工作空间外（用户级 `.workbuddy/gen_backup_tmp.py`），用后删除，避免被自身备份捕获。
- ⚠️ Git 全停令：未 add 未 commit。

## ① 信息治理铁律4 拍板：CONVENTIONS 5.6「删前必回档+用户确认」失效（2026-08-25 14:11）
- **用户裁决**：5.6 的「删前必回档+用户确认」已失效——① 有 backup.md 第2铁律（全空间逐字符备份）取代"删前回档"；② 有「文件改动铁律」（首次明确同意前不修改/删除任何文件）覆盖"用户确认"。
- **落地**：
  1. `CONVENTIONS.md` 5.6：保留"处置出口/禁止裸放 md 防漂移"核心（backup/确认都管不到），删除"删前须回档"子句与"删前可回档"措辞，删除纪律段改标"已精简·原删前必回档+用户确认已失效"，说明回档/确认分别被第2铁律/文件改动铁律取代。
  2. `MEMORY.md` 信息治理铁律4：由"关系待明确（待确认①）"改为"5.6 删前回档+确认已失效：回档归 backup.md 第2铁律、确认归文件改动铁律；5.6 仅留处置出口/禁止裸放核心"。待确认清单 2→1 项（删 ①），现：②禁止指向边界。
- **第 2 铁律执行**：受管文件（MEMORY.md/CONVENTIONS.md）变更 → 重生成 `memory/backup.md`（方案 C2，agent=ProductManagementExpert，files=92）。比对：CONVENTIONS 5.6 段"删除纪律（已精简，2026-08-25）"逐字一致；MEMORY 段待确认仅剩 ②、信息治理铁律4 已更新，逐字一致。排除正确。
- **踩坑（备份脚本）**：Write 工具与 Bash 看似不同 FS 视图——Bash 多次"文件不存在"；最终用 Write 写完整脚本到项目 `backups/`（内含 `gen_backup_tmp.py` 排除项）→ 单独 Bash 运行 → 再单独 Bash 删除，files 稳定=92。heredoc 写 `/tmp` 因 Git Bash 当前盘 D: 解析为 `D:\tmp` 而失败，改项目路径解决。
- ⚠️ Git 全停令：未 add 未 commit。

## ② 禁止指向边界结案 + README 公开暴露铁律违规发现（2026-08-25 14:34）
- **② 结案**：用户指出"README 按规范只给外人读，不可能出现 CONVENTIONS.md，为何还会有指针"——前提（规范层）成立，但验证发现**当前 README.md 违反公开暴露铁律**：L11/13/76/80/88/94/117/126/249 多处写 `.workbuddy/skills/.../SKILL.md`、`.workbuddy/docs/CONVENTIONS.md`；L204-210 整棵树画出 `.workbuddy/` 目录。
- **② 结论**：README 不归"禁止指向"铁律管，由"公开暴露铁律"全禁内部引用（故 ② README 子项多余，关闭）；SKILL 去重指针由铁律2「去重指针除外」已覆盖。待确认 1→0 原项；新增 ③ README 违规清理（待拍板）。
- **MEMORY 瘦身**：为将 ② 结案 + ③ 记入，MEMORY 一度 3039 字符（超 3000 注入上限，会触发最高无上铁律）→ 5 处精简（砍冗余措辞、保留铁律实质）压回 2959，注入安全。
- **第 2 铁律执行**：受管文件（MEMORY.md）变更 → 重生成 `backup.md`（C2，agent=ProductManagementExpert，files=92）；5 处精简逐字入备（710/715/740/754/760 行），索引无残留脚本。
- ⚠️ Git 全停令：未 add 未 commit。README 违规清理属文件改动，待用户拍板后另处。

## ③ README 公开暴露铁律违规清理完成（2026-08-25 14:49）
- 用户拍板策略：直接删内部块 + 目录树同时去 `.workbuddy/` 与 `.build/`。
- 删除：跨设备权威源约定(L11)、必读 SKILL.md(L13) 两 blockquote；3.1 专家转介纪律节(L84-88)，并重编号 3.2→3.1；SKILL.md SOP 指针 6 处(L76/80/94/117/126/249)+裸 SKILL.md(L99)；专家转介交叉引用(L107/L124)改写去内部术语。
- 目录树：去 `.build/`(L179-184) 与 `.workbuddy/`(L193-199) 子树，修正 `├── assets/`→`└── assets/`。
- grep 验证：`.workbuddy`/`CONVENTIONS`/`SKILL.md`/`专家转介` 全 0。
- 残留：`assets/.build/*.py` build 命令多处（quick-start/数据维护/FAQ/上线清单），不属 `.workbuddy/` 违规，但 v5 曾刻意隐藏 `.build/` 名 → 列为 ④ 待拍板。
- MEMORY 待确认 ③→④（记入 `.build/` 脱敏待拍板）。
- 第2铁律：受管文件 README/MEMORY 变更 → 重生成 backup.md（files=92）。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY.md 整理（2026-08-25 15:11）
- 用户指令：删 MEMORY.md 冗余"2026-08-25 升级"（无意义、浪费 3000 字符注入名额）+ 整体整理。
- 执行：去第2铁律标题"2026-08-25 升级"、L50"（2026-08-25 用户确认）"→"（用户已确认）"、L63"已失效（2026-08-25）"→"已失效"。释放约 36 字符（2998→2962），注入安全。
- **第一铁律延伸提案（已否决）**：原拟加"注入护栏"硬上限子条款（写入前自测≤3000、超限先瘦身）。用户否决 → 改 3000 软上限（见 15:20 节）。
- 第2铁律：受管文件 MEMORY/changelog/日志 变更 → 重生成 backup.md（files=92）。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY 3000 软上限定稿（2026-08-25 15:20）
- 用户决策：放宽 MEMORY 写入 3000 限制——写入不强制 ≤3000（可超）；超 3000 仅记为待办"自动注入将截断，需瘦身"，不拦截、不反复精算字符（降过程量/token 消耗）。否决了上轮"注入护栏"硬上限提案。
- 落笔：第一铁律下补「3000 软上限」子弹（写入不强制≤3000；超 3000 仅记待办"注入将截断，需瘦身"，不拦截、不反复精算字符）。
- 过程优化：本轮回不再跑字符统计 python、不再为压 3000 反复精简；仅按第2铁律重生成 backup.md 一次。
- 第2铁律：受管文件 MEMORY/changelog/日志 变更 → 重生成 backup.md（files=92）。
- ⚠️ Git 全停令：未 add 未 commit。

## README 改写为手工写站指南（2026-08-25 15:35）
- 用户拍板三项：①重写（10 章节手工写站指南）②删除内部统计/广告真实 ID（百度双 id/GA4/AdSense/slot）③保留手工视角目录树（去 build 标注+去 json/xlsx 行）。
- 删除：决策与偏好总览整章（状态标记/可接受清单/PENDING/BACKLOG）、操作铁律 blockquote、快速开始 py 构建块、站点配置（build_homeplus.py）、数据维护（xlsx）、统计与广告真实 ID、技术要点备忘 py 内部、v5 变更记录内部、FAQ 中 xlsx/build 引用。
- 改写：xlsx 列定义 → "在 HTML 里手写这些值"字段指引（第五节）；骨架/交互/SEO/部署保留对外有用知识；目录树去 build/json/xlsx 行。
- grep 验证：`.workbuddy`/`.build`/`python`/`xlsx`/`self_meta`/`manifest`/`collect_meta`/`check_links`/真实统计 ID 全 0。
- MEMORY 待确认 ④→无（重写替代 .build/ 脱敏）。首读锚点 ② 由"决策与偏好总览"改为"公开层手工写站指南"。
- 第2铁律：受管文件 README/MEMORY/changelog/日志 变更 → 重生成 backup.md（files=92）。

## changelog=时间日志去重浓缩 成文（2026-08-25 16:05）
- 用户设定（拍板"写入"）：changelog 记录变化范围=L12（同 backup 全空间受管文件）；时间日志=changelog 的时间片段（time_log ⊂ changelog）；changelog=所有时间日志的去重浓缩（∪dedup）。
- 落笔：MEMORY「文件职责边界」段新增 changelog.md / 时间日志 两条职责；此前我误把时间日志理解成"蒸馏进 MEMORY+changelog 平行摘要"，用户纠正为包含关系。
- 第2铁律：受管文件 MEMORY/changelog/日志 变更 → 重生成 backup.md（files=92），新职责逐字入备（L733/734/757），临时脚本已删。
- 注：MEMORY 增至 3171 字符超 3000 软上限，已按软上限铁律记待办"需瘦身"（不拦截、不精算）。
- ⚠️ Git 全停令：未 add 未 commit。

## 备份/审计指令 改造（2026-08-25 16:59）
- 用户拍板三项：① 第2铁律通则改"受管文件变更→实时记时间日志；仅备份指令时重生成 backup.md"；② 信息治理铁律3 改"时间日志兜底（实时层），changelog 为定期蒸馏层"；③ 新增「备份/审计指令」定义段（备份=快照+changelog 追加；审计=三重验证 backup 完整性/changelog 覆盖性/时间日志支撑性）。
- 逻辑修正：用户原设想"changelog 蒸馏结果=backup.md"不成立（backup=内容态全量镜像，changelog=事件态变更史，不可相等）→ 改为"覆盖范围一致（均 L12）"。
- 新执行模式：本次 MEMORY 变更**仅记时间日志，未重生成 backup.md**（等备份指令）——新铁律首次生效执行。
- ⚠️ Git 全停令：未 add 未 commit。

## 时间日志生命周期 确立（2026-08-25 17:20）
- 用户拍板三项：1乙（不追溯历史，规则自 08-26 起执行，既有日志保持现状）+ 2乙（历史 backup 版本实体存 `backups/` 带日期副本，changelog 用指针不全文嵌入，去重指针合法）+ 3甲（changelog 滚动保留上一版 backup 指针作基线锚点）。
- 落笔：MEMORY「备份/审计指令」段后新增「时间日志生命周期」段（结构/清理合并/历史版本/changelog 三态）。
- 结构规则：最旧日志顶部=当时 backup 快照+当日记录；后续日志=仅变更记录。
- 清理合并：合并旧日志为归档文件（如 `2026-08-20-23.merged.md`），合并后蒸馏 changelog 须信息等价。
- ⚠️ Git 全停令：未 add 未 commit。

## 时间日志生命周期 补充·首日志前置动作（2026-08-25 17:27）
- 用户拍板 A：时间日志顶部**只嵌指针**（指向 backups/ 日期实体副本），不全文嵌入（防日志膨胀 1.3MB+）。
- 落笔：MEMORY「时间日志生命周期」段补「新体系首日志前置动作（08-26 首日必做）」——① 先执行备份指令（生成含 25 号全部变更的新 backup.md）② 实体副本存 `backups/backup-20260826.md` ③ 建 26 号日志顶部嵌指针 ④ 其余正常记变更。
- 待办区补「⏳ 08-26 首日必做」提醒（跨会话不遗忘）。
- ⚠️ Git 全停令：未 add 未 commit。

## 安全边界段 删除（2026-08-25 17:34）
- 用户拍板 A：整条删除 MEMORY「安全边界」段（原 L41-42）。
- 判定依据：内容大部分被「跨设备数据策略三件套」覆盖（用户级技能=策略①、用户级记忆=策略②、身份软链=策略③）；唯一独立信息（workbuddy.db/mcp.json/受管 node·python 保持本地）是空间结构天然事实（均在 `~/.workbuddy/` 工作空间外），非行为规则，按信息治理铁律①冗余即删。
- ⚠️ Git 全停令：未 add 未 commit。

## .gitignore 同步记录铁律 迁入 CONVENTIONS（2026-08-25 17:36）
- 用户判定：该段属成文约定（读者含用户），应归 CONVENTIONS.md（单一权威源），不应占 MEMORY 注入名额。
- 执行：MEMORY 删「.gitignore 同步记录铁律」段（原 L77-80）→ CONVENTIONS 5.2 后新增「5.2.1 .gitignore 同步记录铁律」，措辞同步新通则（受管文件增删改→实时记时间日志，备份指令时重生成 backup.md 比对）。
- 附带收益：MEMORY 再减一段（约 -200 字符），瘦身待办推进。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY/CONVENTIONS 架构整理（2026-08-25 17:46）
- 用户架构主张：MEMORY 只留步骤（动作指令），解释"为什么"归 CONVENTIONS；CONVENTIONS = MEMORY 扩展版，内容互查（MEMORY ↔ CONVENTIONS §6）。
- 用户拍板：① 项目背景整段移入第六章（不留 MEMORY）② CONVENTIONS 追加第六章，原内容可并入六章的就并入。
- 执行：MEMORY 重写为纯步骤版（删项目背景段、"三层证据链"解释句移 6.4、冗余措辞合并）；CONVENTIONS 追加「六、MEMORY.md 扩展解读」（6.1 项目背景 / 6.2 最高铁律 / 6.3 backup / 6.4 备份审计 / 6.5 时间日志 / 6.6 首读锚点 / 6.7 跨设备 / 6.8 职责边界 / 6.9 文件改动 / 6.10 Git 同步 / 6.11 公开暴露 / 6.12 对话翻译 / 6.13 信息治理），原一~五章被覆盖小节加"见 §6.x"指针（不删除，保留操作细节），站点技术类（JSON-LD/面包屑/专家转介/流程执行）保留。
- MEMORY 现 3362（仍超 3000 软上限，待办挂起）；本次为架构整理非压字数，未反复精算。
- ⚠️ Git 全停令：未 add 未 commit。

## CONVENTIONS 重构：MEMORY 章节前移 + 合并重复（2026-08-25 17:51）
- 用户指令：整理 CONVENTIONS.md——把 MEMORY 扩展解读章节前移为第一章，合并录入重复内容进 MEMORY 章节。
- 执行：CONVENTIONS 整文件重写——原「六、MEMORY 扩展解读」→ 新「一、MEMORY.md 扩展解读」（1.1~1.13）；原「一、跨设备权威源约定」并入 1.7；原 5.2 同步强制 + 5.2.1 .gitignore + 5.5 冲突裁决并入 1.10；原 5.4 首读锚点并入 1.6；原 5.6 文档生命周期并入 1.13；保留二专家转介/三 JSON-LD/四面包屑/五流程（5.1 迭代 + 原 5.3 归档，重编为 5.2）。
- MEMORY.md 指针同步：§6→§1（头部互查、§1.10、§1.13 三处），grep 验证零残留。
- ⚠️ Git 全停令：未 add 未 commit。

## MEMORY 删信息治理铁律4（历史记录不入 MEMORY）（2026-08-25 17:56）
- 用户判定：信息治理铁律 4（"原 CONVENTIONS 5.6 删前回档+确认已失效…"）是**历史变更记录**，不是可执行步骤——不应占 MEMORY 注入名额。
- 归位逻辑：失效声明已在 CONVENTIONS §1.13（文档生命周期完整版）写明；"回档归 backup 第2铁律/确认归文件改动铁律"本就写在 backup 铁律与文件改动铁律自身；"5.6 失效"历史已在 changelog（① 条目）与今日日志。三处落点均已有，MEMORY 无需重复。
- 执行：删 MEMORY 信息治理铁律第 4 条，仅留 1-3（冗余即删/完整复制/时间日志兜底）三条活规则。
- ⚠️ Git 全停令：未 add 未 commit。

## 按"历史记录归位"逻辑整理三文件（2026-08-25 17:57）
- 用户指令：按刚才逻辑（历史记录不入 MEMORY/权威源，归记录层）整理 CONVENTIONS.md、MEMORY.md、2026-08-25.md。
- CONVENTIONS.md：第一章（MEMORY 扩展解读）删 3 处纯历史记录——1.6「md 精简结果（历史）」、1.10「.gitignore 变更记录展开句」（保留"全文权威载体=backup.md"与"原因备忘见 backup 注释"）、1.11「曾清理 13 处…重写」事件句；均已在 changelog/日志有完整记录，不丢信息。
- MEMORY.md：待办区措辞更新（"整理后应达标"→"现约 3270，需瘦身"）。
- 2026-08-25.md（本文件）：定位=时间日志（L2 原始事件流），append-only 不改历史；确认历史归位在此正确。
- ⚠️ Git 全停令：未 add 未 commit。

## 备份指令执行·重生成 backup.md（2026-08-25 18:54 用户指令"开始备份"）
- 触发「备份指令」（MEMORY「备份/审计指令」段）：① 重生成 backup.md ② changelog 追加。
- 诊断：磁盘受管文件 89（排除 .git/、changelog.md、时间日志×2、backup.md 自身）；旧 backup.md 索引 92，差集 3 个均为已不存在的 `backups/` 旧回档 → `backups/` 当前为空。
- 第2铁律执行：Python 脚本（openpyxl 转 xlsx）重生成 `memory/backup.md`（方案 C2，files=89，agent=ProductManagementExpert）；文本逐字拷贝+来源注释、xlsx 转文本表、二进制登记。
- 校验：子串校验 VERIFY OK（count=89）——文本无遗漏、排除正确、无时间日志泄露；初版正则校验因 SKILL.md/README.md 含 ``` 代码块误报 2 mismatch，子串法确证为假阳性。
- changelog 追加 ⑮（本次重生成记录）。
- ⚠️ Git 全停令：仅写文件（backup.md/changelog.md/本日志），未 add 未 commit。

## MEMORY/CONVENTIONS Git 同步纪律去矛盾（2026-08-25 19:31）
- 用户指出 MEMORY「Git/同步纪律」段两处问题：① 标题"完整版见 CONVENTIONS §1.10"指向过时矛盾内容，无意义；② 第51行"强制同步：…"概念已被全停令废止、且文件清单（身份4/docs/*/memory/*/skills/**/站点源码）与跨设备三件套重复。
- 用户拍板：MEMORY 第51行删除（非移 CONVENTIONS）；CONVENTIONS §1.10 一并修正（两问均选推荐）。
- 执行 MEMORY：删标题"完整版见 CONVENTIONS §1.10"指针、删第51行"强制同步"整行；Git/同步纪律段现仅留 全停令+提醒规则（grep 验证零残留）。
- 执行 CONVENTIONS §1.10：标题改为"含全停令/冲突裁决/.gitignore 政策"；"同步强制规则（完整版）"标【已废止】、明其被全停令（含 add 也禁）覆盖失效；"严禁进 git"保留为 .gitignore 政策（仍有效）；冲突裁决改为"全停令优先、旧 add≠commit 裁决失效"；删 §1.10 内"每次收口必查补 add""新会话补 add"等过时指令。第6行、第197行"同步强制"引用改为"Git 全停令"。
- ⚠️ Git 全停令：仅写文件（MEMORY.md/CONVENTIONS.md/本日志），未 add 未 commit。

## card--t1 还原为最初 v4.1 结构（2026-08-25 20:54 用户指令"改回原有的t1"）
- 背景：本会话早段将 card--t1 改为 E 方案（5行：图标仅1行+描述满行固定2行纵向滚动+VSCROLL 悬停锁定滚轮），用户最终要求还原。
- 还原 `assets/css/style.css` `.card--t1`：网格由 5 行改回最初 4 行（媒体跨 1-2 行、描述占第2行右两列、标签第3行、链接第4行）；删除 t1 desc 的 `white-space/overflow-y/min-height/max-height` 纵向滚动覆盖，恢复为 `.card__desc { grid-area:desc; align-self:start }`（继承通用 `.card__desc` 的 nowrap 单行横滚）。
- 还原 `assets/js/main.js`：SCROLL_ROW_SEL 加回 `.card__desc`（横向滑道）；删除 VSCROLL_SEL / vScrollRows 声明、refreshScrollable 内纵向检测块、wheel 处理的纵向分支（鼠标移出解锁逻辑一并撤销）。
- 验证：node --check 通过；grep 无 VSCROLL/scrollTop/max-height 残留；通用 `.card__desc` 仍为横向拖拽滑道（line 103-108）。
- ⚠️ Git 全停令：仅写文件（style.css/main.js/本日志），未 add 未 commit。

## 代码检查问题修复（A+B，2026-08-25 21:18 用户指令"A、B"）
- 背景：用户 20:54 还原 t1 后，要求执行此前评估列出的 A、B 两项修复（C=xlsx→CSV 迁移未授权，未做）。
- A. main.js 两处：
  1. 删除第277行残留注释「E 方案：卡片描述改为纵向滚动，移出横向滑动接管…」（t1 已还原为横向滑，注释矛盾误导维护）；SCROLL_ROW_SEL 含 `.card__desc` 无误。
  2. `highlightCard` 加 `escapeHtml` 转义：文本经 Python html.escape 写入源 HTML→浏览器 textContent 解码回 < & → 筛选高亮 `innerHTML` 重插会破损/注入。改为先 escape orig 与 keyword、仅保留 `<mark>` 包裹 → 不再破坏含 < & 内容。无关键词分支仍用 `textContent` 安全赋值。
- B. check_links.py 改为按表头名解析：
  - 删除 `from openpyxl import load_workbook` 与本地 `parse_links` 副本、删除 `collect_urls` 里 `row[7]` 硬编码列索引（links 第8列）；改为 `sys.path.insert` 后 `from build_homeplus import load_rows, parse_links`，复用与 build 同一套表头名解析 + parse_links。
  - 收益：xlsx 列顺序调整后死链检测不再静默读错列；构建与检测解析逻辑统一、消除重复。
- 验证：node --check main.js 通过；py_compile check_links.py + build_homeplus.py 通过；实跑 load_rows/parse_links 解析真实 self_links.xlsx = 128 卡片 / 10 分类 / 153 链接，无误。
- ⚠️ Git 全停令：仅写文件（main.js/check_links.py/本日志），未 add 未 commit。

## agent 团队接力评审问卷（2026-08-25 21:47 闻真专家会话，用户指令"设一份md给agent团队"）
- 背景：用户不要真实用户访谈，改为给 agent 团队（产品/开发/代码审查/UI/独立站/SEO/搜索词/用户研究/体验架构等，不限）的接力评审问卷，让各 agent 以"普通用户+本专业"双重身份作答。
- 产出：新建 `agent-review-survey.md`（项目根目录，v1.0）。
- 问卷结构：0 接力使用说明（规则：只评方案不评人/修改必须可落地/鼓励互相挑刺/不设维度限制/可实站验证）；1 项目背景（公开层信息，不含 .workbuddy）；2 评审目标（看得懂吗/哪吸引哪劝退/怎么改更好用可信留人增长）；3 通用问卷（用户画像/场景现状/第一印象/体验意愿，以普通用户身份作答）；4 专业深挖（自拟 3 题自答 + 9 专业参考方向表）；5 三件套（点评/修改方案/发展方向）；6 推荐下一位 + 建议接力顺序；文末答卷追加区。
- 关键设计：每份答卷末尾必须点名推荐下一位 agent，形成接力链；专业深挖题用来加维度/深度/广度。
- ⚠️ Git 全停令：仅新建 agent-review-survey.md，未 add 未 commit。

## agent 接力问卷升级 v2.0 圆桌版（2026-08-25 22:02 用户细化接力机制）
- 用户要求：问卷从"问答接力"升级为"圆桌讨论"。每位 agent（B）流程：①作答通用问卷 → ②读上一位（A）答卷 md → ③综合 A+B 写圆桌总结（认同/补充/反对/再推一步）→ ④自拟 3 题自答并把 3 题追加回主问卷第 6 节「历届追加问题池」→ ⑤点名下一位（C）+理由 → ⑥合并生成独立 md，命名 `{第几个作答}-{agent名称}-{YYYY-MM-DD}.md`，含带题目的作答/圆桌总结/3题及答案/下一位名称，并在主问卷第 7 节「答卷索引」登记。
- 执行：重写 `agent-review-survey.md` 为 v2.0（第 0 节圆桌规则、第 5 节答卷 md 模板、第 6 节问题池、第 7 节答卷索引；第一位无上一位跳过综合）。
- ⚠️ Git 全停令：仅写 agent-review-survey.md + 本日志，未 add 未 commit。
- **用户修正（22:08）**：不是看"上一位"，而是看**之前所有答卷**——圆桌机制改为"读全"：每个 agent 先读第 1 份到最新一份全部答卷，再综合写圆桌总结。已改：流程第 2/3 步、规则首位置说明、模板头部（上一位→已阅答卷）、模板二节标题与说明，grep 验证零残留。
- **问卷迁移（22:10，用户指令）**：问卷与答卷统一放 `.workbuddy/agent-review-survey/`（不随公开站点暴露）。已建目录并把 `agent-review-survey.md` 从根目录移入，问卷内相对文件名引用无需改动。
- **第 1 份答卷（22:11，用户指令"你第一个作答"）**：闻真（用户调研专家）作为圆桌第 1 位作答，产出 `.workbuddy/agent-review-survey/1-用户调研专家-2026-08-25.md`。核心发现：① 站点定位漂移——meta/OG/JSON-LD 声明"政协政务垂类"，首页实际 10 类通用导航（机构/政协/资讯/AI/设计/开发/学习/效率/影音等 128 卡），两套人设打架；② 收藏 localStorage 不跨设备=信任硬伤；③ 无"没找到"兜底反馈。3 个问题已入问题池，推荐下一位=产品专家。已同步更新主问卷第 6 节问题池 + 第 7 节索引。
- **第 2 份答卷（22:19，专家切至产品通，用户指令"作答问卷"）**：产品专家作为第 2 位作答，产出 `2-产品专家-2026-08-25.md`。核心判断：① 主定位选垂类"可信政务/机构入口"，全量通用降为辅助层（通用导航无护城河）；② 北极星=周回访率，驱动=搜索/筛选使用率、任务成功率、收藏使用率，健康=扑空率；③ 增长飞轮=收录增量→新鲜感→回访→反馈需求→收录增量，用"本周上新"做钩子、政务时效更新是天然回访理由。3 问已入池，推荐下一位=网站开发专家。
- **答卷命名统一（22:22，用户选"改为产品管理专家"）**：第 2 份答卷及主问卷问题池/索引/附录顺序中的"产品专家"全部改为"产品管理专家"；文件重命名为 `2-产品管理专家-2026-08-25.md`；第 1 份答卷中闻真的"推荐下一位：产品专家"为历史原话保留。
- **第 3 份答卷（22:24，专家切至鹏城信息AI专家/网站开发专家，用户指令作答）**：产出 `3-网站开发专家-2026-08-25.md`。基于实读代码（main.js 621 行/style.css 800 行/index.html）评审：① 工程质量高（渐进增强/escapeHtml 防注入/防抖/预计算/a11y 基础齐全）；② 技术债=广告 CLS 风险、128 卡单行超长 HTML 首包大、外部图片不可控、user-select:none 禁复制、长标题横滚移动端难用、滚动按钮组隐藏态可被 Tab 聚焦；③ 回应产品方案：反馈入口用托管表单、收藏导出纯前端可行、云收藏需后端（先导出过渡，验证后再上 Workers+KV/Supabase）；④ 新观察=浏览器地址栏自动补全正在替代导航站，佐证通用导航无护城河。3 问已入池，推荐下一位=代码审查专家（滚动按钮锁机制状态机复杂度高需核查）。
- **第 4 份答卷（22:28，专家切至火眼眼/代码审查专家，用户指令作答）**：产出 `4-代码审查专家-2026-08-25.md`。审查全部代码后的发现：① 🔴 搜索高亮标签破损 bug（main.js highlightCard 连续 replace 会二次命中 <mark> 标签文本，如搜"k"/"mark" 产生 <mar<mark>k</mark>>）；② 🔴 favToggle.addEventListener 无判空（engine 判了它没判，页面缺元素整段脚本崩）；③ 🟡 搜索大小写敏感（搜 github 找不到 GitHub）；④ 🟡 亮色 focus-visible 亮金 outline 对比度 ~1.9:1 不达 WCAG 2.4.7；⑤ 🟡 background-clip:text 无回退（老浏览器 hero 标题不可见）；⑥ 澄清上一位"滚动按钮隐藏态可被 Tab 聚焦"不成立（display:none 不参与 Tab 序）。安全面结论：XSS 面基本封闭无注入路径。3 问已入池，推荐下一位=UI 设计专家。
- **第 5 份答卷（22:34，专家切至像素君/UI设计师，用户指令作答）**：产出 `5-UI设计专家-2026-08-25.md`。视觉层评审：① 认同定位漂移且指出"视觉本身就是定位表达"（24 岁普通用户人设第一眼被正式气质劝退）；② 给出代码审查遗留问题的视觉方案：亮色 focus 换深金 #B08D3E（~4.6:1 达标）、background-clip 加 @supports 回退纯红；③ UI 专属发现：广告区与内容区隔弱（12px 灰字易误认内容，伤信任）、卡片标题横滚"断在半空"无省略号、空状态缺引导；④ 奢华=克制：金色必须小面积点缀、hover 动效别超过 2 层；⑤ 暗色暖黑金质量高，建议暗色描边用半透明金。7 项 CSS 级修复清单（焦点环/广告区隔/两行截断/字号 13→14px/去 user-select/暗色描边/空状态引导），另建议整理设计 token 文档作"视觉宪法"。3 问已入池，推荐下一位=独立站专家。
- **第 6 份答卷（22:41，专家切至独立站SEO专家，用户指令作答）**：产出 `6-独立站SEO专家-2026-08-25.md`。核实 robots/sitemap/ads/频道页 head 后评审：① 变现与信任冲突=最大商业风险（AdSense 无差别广告，代办中介类广告是政务向信任杀手；共存三原则=可预期/可区隔/可豁免，政务频道页不放广告）；② SEO 技术面扎实（sitemap 分层/CollectionPage JSON-LD/robots 白名单全齐），但定位漂移=关键词资产分裂（meta 与首页内容两套词，权重无法聚焦）；③ 新增缺口：ICP 备案缺失（国内可达/微信内拦截/官方感三重缺口）；④ 内容资产为零（无 blog/news/journal，吃不到"办事指南"搜索流量）；⑤ 方案：SEO 定位对齐、广告信任分级、备案评估立项、频道页导语内容化（300-500 字）、"官方认证"标记（政务卡片徽标+核验时间）、blog 办事指南文章+FAQPage。3 问已入池，推荐下一位=SEO 搜索专家。
- **第 7 份答卷（22:54，专家切至搜霸霸/SEO专家，用户指令作答）**：产出 `7-SEO搜索专家-2026-08-25.md`。SEO 视角评审（核实 robots/sitemap/频道页导语现状）：① 技术 SEO=A 级（robots 白名单/sitemap 分层/canonical/WebSite+SearchAction/CollectionPage 全齐），三个加分项未做=图片 alt 全空、缺 Organization/BreadcrumbList JSON-LD、缺 FAQPage；② 关键词资产分裂落地为战略：主集群="政务导航/官方入口导航/XX官网直达"，全站 title/desc/h1/锚文本对齐，通用分类停止抢词；③ 导航站困境=卡片页拿不到排名，出路=频道页导语内容化（现有 channel-intro 仅一两句话，扩到 300-500 字+FAQ）；④ 百度主战场五件套=主动推送/原创度/移动端/行为信号/防"低质目录站"判定；⑤ 数据纪律：无真实数据不虚构搜索量。3 问已入池，推荐下一位=搜索词分析师。
- **第 8 份答卷（22:59，专家切至词探探/搜索词分析师，用户指令作答）**：产出 `8-搜索词分析师-2026-08-25.md`。搜索词视角评审：① 核心发现=分类名是"站方语言"不是"用户语言"（用户搜"医保局/下载/网上办理"，站点叫"机构/政务服务平台"），这是站内扑空根因，需建"用户语言↔站点语言"对照表；② 站内搜索无打点=最便宜的免费 query 数据源被浪费（GA4 埋搜索词+结果数+扑空标志，周报高频 Top50+扑空 Top20，扑空词驱动收录优先级）；③ 假设库（标注待数据验证）：导航型 query（{机构}+官网/入口）→卡片直达，信息型 query（{事项}+网上办理/入口在哪）→频道页导语+指南文章；④ 负面词隔离三组：代办黄牛类/金融骚扰类/下载器类，内容与广告双线不碰。数据纪律：无真实数据不编数字。3 问已入池，推荐下一位=用户体验研究员。
- **第 9 份答卷（23:04，专家切至探真真/用户体验研究员，用户指令作答）**：产出 `9-用户体验研究员-2026-08-25.md`。研究视角评审：① 核心立场=8 份答卷全是假设无真实用户数据，先验证再动手（3 周最小验证：5 人访谈痛点+10 找站任务可用性+站内打点需求频率，判据=痛点 4/5+任务完成率≥70%+打点有量）；② 归纳 8 位人设的反复模式（标注待验证）：怕钓鱼 6/8、收藏夹乱 5/8、百度广告污染 7/8、设首页意愿政务 4-5 分 vs 年轻 2-3 分、第一印象全正面=视觉可信感是既有资产；③ 站内搜索=第二入口，任务测试验证搜索词分析师的"用户语言对照表"；④ 扑空→机会三步闭环（反馈出口/扑空词驱动收录/对照表修正）。3 问已入池，推荐下一位=用户体验架构师。
- **第 10 份答卷（23:08，专家切至体验达/用户体验架构师，用户指令作答，圆桌首轮收口）**：产出 `10-用户体验架构师-2026-08-25.md`。架构视角评审 + 整体收口：① IA 诊断=现在是"分类中心"，用户 80% 是任务型，解法=双模式 IA（首页加"我要办事/查政策/找工具"任务入口，与分类浏览并存）；② 分类（快捷筛选）与频道页（垂类内容页）分工脱钩，消除两套组织心智负担；③ README 骨架契约（S1-S6）是稀缺资产，升级为"页面契约文档"+ 预定义 S6 内容页契约；④ 收口整合 9 份答卷为四优先改造路线图：P1 清 bug+UI 修复+搜索打点（1-2 周）→P2 最小验证+定位文案+频道页导语（3-4 周）→P3 双模式 IA+收藏导出+反馈入口+广告分级（1-2 月）→P4 备案+内容矩阵+认证标记+契约文档（2-3 月+）。3 问已入池，推荐下一位=产品管理专家（第二轮收口轮）。
- **第 11 份答卷（23:14，专家切至鹏城信息AI专家/市场分析专家，用户指令作答）**：产出 `11-市场分析专家-2026-08-25.md`。外部市场视角（前 10 位全是内部视角）：① 市场判断=通用导航是夕阳、垂类导航是朝阳；"政务/办事入口可信导航"是真实但未验证的细分机会（政务数字化红利+搜索广告污染+无强竞品三条件成立）；② 竞品三层：直接竞品≈无（蓝海）、间接=浏览器/搜索/微信搜一搜（真正的对手）、同类参照=AI 导航站可行+hao123 衰落；打法=不正面打"导航"打"可信入口"；③ 定位漂移=市场认知混乱（传播的前提），一句话定位="政协与政务办事入口的可信导航"；④ 冷启动=不是流量生意是信任生意，聚焦关键人（政协/政务/社区工作者）微信渠道，3 周验证为市场决策闸门；⑤ 风险提示=政务内容合规敏感需审查。数据纪律：竞品/趋势为公开常识判断，无一手市场数据。3 问已入池，推荐下一位=产品管理专家（第二轮收口）。
- **第 12 份答卷（23:21，专家切至深度思考引导专家，用户指令作答）**：产出 `12-深度思考引导专家-2026-08-25.md`。唱反调/挑战共识角色，指出三个"房间里的大象"：① 品牌/定位/内容三层错位（名字"正协"锁政协、共识定位政务、内容全量通用，品牌决策被回避；建议先按"正协=政协垂类"深做验证再评估改名）；② 愿景"不止于找到"与全部方案（都在优化"找到"）断层，无落地物（建议先兑现"找到官方入口且确信是真的"=认证标记+核验时间）；③ 集体确认偏误风险（11 份答卷无真实数据，逻辑自洽≠市场对，需反对派评审+3 周验证背书）。动工前补"why"：品牌拍板/愿景落地物/反对派评审。3 问已入池，推荐下一位=产品管理专家（第二轮收口）。
- **第 13 份答卷（23:26，专家切至深研研/深度研究专家，用户指令作答，6 轮真实外部研究）**：产出 `13-深度研究专家-2026-08-25.md`。研究验证圆桌假设，关键新事实：① 证实政务数字化红利（国家政务服务平台 521 万服务事项/9.4 亿在线用户/EGDI 全球 35 位）；② **新事实=官方自己在做政务导航**（国家政务服务平台链接区覆盖 46 部门+31 省），正面做政务导航必输，机会在"官方的缝隙"：政协/统战垂类（省级以下官网无聚合）+ 市县级政务网站 + 可信甄别；③ **新事实=仿冒政务网站实锤痛点**（2025 年网信办处置仿冒网站 1418 个，同比 +1.7 倍，真实诈骗案例多人损失数千至万元）——"可信甄别"有硬数据支撑，建议做成域名核验+认证徽标+防仿冒提示+举报联动的功能规格；④ 证实垂类导航可行（AI 工具集月访问 150 万等 15 站）+ 内容化出路（DeepSeek 文章日 IP 100→4000 案例）；⑤ 长期预警=一网通办迈向一网智办（AI 政务助手），导航站可信入口价值长期被替代风险，应对=把人工核验入口数据库做成 AI 时代数据资产。答卷含 References。3 问已入池，推荐下一位=产品管理专家（第二轮收口）。
- **第 14 份答卷（23:33，专家切至求助导航员/心理求助导航顾问，用户指令作答）**：产出 `14-心理求助导航顾问-2026-08-25.md`。独特视角=民生求助类入口是"可信甄别"的极端试金石：① 若收心理援助（12356 2025.5 全国并网）/法律援助/儿童保护类入口，须设"最严核验档"（官方源交叉核验+核验日期标注+季度复核），错误号码耽误求助；② 求助导航核心洞察="求助延迟主因是怕找错怕贴标签而非不愿求助"，与导航站"可信降低怕找错心理成本"同构，政务/民生入口可信价值更深；③ 内容红线前置五条：不做风险评级/不编造机构号码/未成年人受侵害提醒报告义务（未保法第11条）/不协助私了/危机话题转渠道；④ 建议内容化优先民生求助指引（心理/法律援助），最有人情味也最易获信任。3 问已入池，推荐下一位=产品管理专家（第二轮收口）。
- **第 15 份答卷（23:38，专家切至守规规/法律合规审查员，用户指令作答）**：产出 `15-法律合规审查员-2026-08-25.md`。合规视角（前 14 位唯一系统性合规审查）：① **最大合规隐患=名字/域名/视觉三重"官方感"无"非官方"声明**（正协≈政协+zhengxie.com.cn+红金官方感，易误认官方，涉《广告法》第4条/冒用国家机关名义，网信办 2025 处置仿冒 1418 个的敏感领域）——规避=页脚/关于页/首页显著"本站为民间导航网站，与任何政府机构无隶属关系"声明+disclaimer 强化+禁用官方标识；② 数据合规=GA4+双百度统计收集个人信息（PIPL"告知-同意"），需隐私政策披露+统计同意机制+表单上线前隐私审查；③ 备案=境内网站法定要求（《互联网信息服务管理办法》），纳入 P1；④ 内容合规红线五条：非官方口吻/无承诺性表述/涉政遵循官方口径/时效免责/未成年人保护。合规清单应纳入路线图 P1（地基）。3 问已入池，推荐下一位=产品管理专家（第二轮收口）。
- **第 16 步·圆桌收口（23:45，专家切回产品通，用户指令"汇总所有问卷整理成清单，我逐条选择，由网站开发专家改代码"）**：产出 `16-产品管理专家-改造清单-2026-08-25.md`——汇总 1–15 号答卷为 47 条可勾选清单，分 6 组：A 合规与定位（5 条，P0 地基）/ B 代码修复（16 条，全可立即执行）/ C 功能新增（8 条）/ D 内容与 SEO（6 条）/ E 验证与文档（6 条）/ F 长期（6 条）。每条含编号/建议/来源答卷/执行方/工作量；~30 条可代码执行。建议顺序：A1+B1–B16 → C1/C2/C7 → 其余按需。用户逐条勾选后由网站开发专家执行。已登记主问卷索引第 16 行。
- **代码执行（23:57，专家切至鹏城信息AI专家/网站开发专家，用户指令"执行A1,A4,B1-B6,B9-B11,B15,B16"）**：已落地 13 项改造并通过校验。改动：assets/js/main.js（B1 高亮用 PUA 占位符防标签破损+B2 favToggle 判空+B3 大小写不敏感+B15 同义词别名搜索）、assets/css/style.css（B4 广告 min-height 防 CLS+B5 焦点环变量深金/B6 @supports 背景裁剪回退+B9 暗色描边半透明金/B10 广告区隔徽章/B11 字号 14px/B16 .ad__label 走变量）、index.html（A1 页脚声明+首页固定声明条+A4 统计授权条）、pages/about 与 pages/disclaimer（A1 非官方声明）、pages/privacy（A4 细化第三方数据披露+更新日期至 8/25）。校验：node --check 通过；check_links 内部死链 0（62 异常全为外部站点，沙箱不可外连，预存）。遗留：B16 星形 SVG sprite（替代 128 处内联）待 build 生成器层处理；A4 统计硬拦截（EU 加载前拦截）待将 head 统计脚本改为按授权动态注入。清单 13 项已勾选完成。
