# 2026-08-27 工作日志

## 引擎频道落地（承接频道结构收敛；产品通视角）
- **决策**：首页"聚合搜索框"（多引擎外跳启动器）长期价值低、且违背"可信政务/机构入口、减法优先、不抢通用词"定位 → 迁移为独立"引擎频道"，移出首页 hero。
- **用户拍板**：① 引擎频道=引擎框+分类卡片（综合/购物/社区知识/视频/开发/工具，共 18 个引擎）；② 首页 hero=仅 logo+标语。
- **实现（改 `assets/.build/build_homeplus.py`，未裸放手写页）**：
  - 新增常量 `ENGINE_CHANNEL="engine"`；`render_and_write` 按 `canonical_path` 末段判定：根页 `hero_search=""`、引擎频道注入 `HERO_SEARCH_BLOCK`、其余频道用专题介绍块。
  - 根页 JSON-LD 移除误导的 `SearchAction`（原指向 `#q=`，随引擎框离场）。
  - 新建 `directory/engine/`（self_meta.json + self_links.xlsx 18 行，由 `assets/.build/_gen_engine.py` 生成），build 自动产出 `index.html`。
  - 同步：hub `directory/index.html` 增补引擎频道卡片 + JSON-LD `numberOfItems=3`；`sitemap.xml` 增补 `directory/engine/`（priority 0.6）。
- **验证**：sitemap 14 条 URL、4 个 directory 条目；根页无引擎框/无 SearchAction/无残留占位符；引擎频道 18 卡+18 链+引擎框+面包屑；hub 链引擎。
- **技术约束**：build 每次运行删除所有 `directory/<name>/index.html` 防残留，故引擎频道必须走生成器正路（带 xlsx），手写页会被清。
- 未执行 git（git 全停令）。

## 待办/未决
- 人大频道范围：当前 12 个（全国+直辖市+7 省），其余省份/设区市是否扩展待用户定。
- MEMORY.md 超 3000 字符上限，需择机精简（不影响功能）。
- 全部新增/修改文件待用户本地 git 提交。

## 引擎频道改名（产品通决策，用户拍板执行）
- **命名结论**：不叫"搜索频道/搜索导航"（撞"不抢通用词"红线，且语义误导非站内搜索）；采用"**引擎导航**"，与兄弟频道"人大导航/政务机构导航"同构，且"引擎"非通用词、诚实表明第三方引擎集合。
- **执行**：显示名 `引擎频道`→`引擎导航`；目录 `engine/` 与 URL 不变。改 `directory/engine/assets/json/self_meta.json`（title+description）+ 手写 hub `directory/index.html`（卡片 title、icon「搜」→「引」、meta description、JSON-LD `name`）；跑 build 重生引擎页。
- **验证**：live 页无"引擎频道"残留；引擎页 `<title>`/h1/面包屑/JSON-LD 均"引擎导航"（10 处）；hub 卡片 icon「引」、title「引擎导航」；引擎框+18 卡完好。

## 外访链接是否带"正协导航"搜索词（待定）
- **产品通建议：不带**。理由：① 品牌填充=搜索操纵，违背"可信优于流量"；② 毁体验（用户点百度却搜出"正协导航"结果）；③ 零真实 SEO 收益。当前实现已正确（卡片→引擎官网首页 nofollow；搜索框→用户输入词），保持即可。
- **状态**：用户已**最终拍板"不带"**（本轮 SEO 独立站专家亦建议不带，结论一致）；外访链接行为维持现状（卡片→引擎官网首页 nofollow、搜索框→用户输入词），不改。此项彻底关闭。

## 首页用 channel_intro 定义（修复 + 落地）
- **根因**：`assets/json/self_meta.json` 第 4 行 `keywords` 末尾**缺逗号**→ JSON 非法；`load_meta()` 损坏时返回 `{}`→根 meta 回退 `ROOT_META` 常量（无 `channel_intro`）→ 根页 `channel_intro` 字段从未被读到，且根分支 `hero_search=""` → 首页 hero 下半部一直空白。
- **用户洞察正确**：`channel_intro` 本就是"页面定义文案"通用块，函数通用，只需根 `self_meta.json` 给字段即可，且**不碰 slogan**（`SLOGAN` 是 py 常量）、与 SEO `description` 解耦、无字数限制。
- **执行**：① 重写根 `self_meta.json` 为合法 JSON，新增独立 `channel_intro`（不复用 description，定义站点：政协/人大/政务机构+检索工具，减法/不抢通用词）；② `render_and_write` 根分支 `hero_search=""`→`build_channel_intro(meta)`。
- **验证**：首页 hero 第89行 slogan 不变、第91行渲染 `<section class="channel-intro">` 定义文案；根 JSON-LD 仍 WebSite、无 SearchAction、无引擎框；引擎(1框+18卡)/人大/政务机构频道无回归。

## 人大+政务机构合并为单一「政务导航」频道（战略咨询顾问决策，用户拍板 C + slug=gov/）
- **战略判断**：圆桌"减法优先/聚流"原则在频道层面不自洽——首页已 65 卡（政协55主导），若再把人大12+政务13 并首页=90 卡（内容堆，违导航入口定位）；且"大类留首页、小类进频道"倒挂。故不并首页，而将人大+政务机构 2 频道**合并为 1 个 `gov/` 政务导航频道**（减法一致、IA 自洽）。
- **执行**：① `assets/.build/_gen_gov.py` 读 renmin+zhengwu 两 xlsx 合并→`directory/gov/assets/xlsx/self_links.xlsx`（25 卡，站序重排）；② 建 `directory/gov/assets/json/self_meta.json`（title=政务导航，含 channel_intro 两类官方入口说明）；③ renmin/zhengwu **归档**至 `.workbuddy/trash/2026-08-27-gov-merge.bak/`（可回档，非永久删）；④ 手写 hub `directory/index.html` 两卡→一卡（政务导航，icon「政」）+ JSON-LD `numberOfItems=3→2` + meta；⑤ `sitemap.xml` 删 renmin/zhengwu、加 gov（priority 0.65）。
- **验证**：build 生成 gov 页 25 卡/25 链、WebPage JSON-LD ItemList=25、面包屑、channel_intro 均到位；全站无 renmin/zhengwu 残留引用；hub→gov+engine、JSON-LD=2；sitemap 13 条、directory 条目=hub+gov+engine；首页 65 卡/无引擎框/无 SearchAction 无回归。live `directory/` = engine, gov, index.html。
- **遗留待办更新**：原"人大频道扩省"待办→现并入"政务导航扩省/扩部委"（需逐省逐部联网核实，不编造）。

## 暗色模式设计原则（朱玺政务，用户确立）
- **用户确立的设计优先级**：暗色模式不是"主题搭配延续"，而是**用户主动切换的动机场景**——亮色首访已完成"可信"品牌暗示，暗色的任务是服务"切换动机"（深夜护眼不刺眼 / 暗色偏好人群差异化获客）。
- **圆桌回响**：#18 品牌策略师"暗色模式复用同一语言，完成度高，固化即可"；#26 数据分析"零埋点=假设不可量"；#12 引导"行为改变成本/暗色非获客主杠杆"；#1 用户调研"留存生死线=扑空率+跨设备收藏"。
- **设计落点（朱玺政务暗色）**：暖炭底(非死黑)·朱砂(非酒红)·记住偏好(localStorage)·上线即埋 `theme_switch` 事件（把"暗色人群留存更高"从假设变可量问题）。
- **范围定调**：暗色=已获客用户的舒适/留存层 + 偏好人群的小差异化获客边；非站顶漏斗主杠杆（主杠杆=可信甄选+设为主页，#12）。
- **状态**：预览稿 ui-redesign-proposals.html 已含该暗色方案；正式写入 style.css 待用户拍板。

## 朱玺政务暗色模式落地（用户拍板①②，已实施）
- **① 配色定稿（动机导向）**：`[data-theme="dark"]` 由"阴森"版（死黑 #0D0C0E + 酒红 #D65A60 + 亮金 #E8CB84）改为朱玺政务（暖炭 #1E1B1F + 朱砂 #E2604E + 香槟金 #D9B978），同步 sticky 半透明底 rgba(30,27,31,.96)。原则写入注释：亮色首访已完成可信暗示，暗色只服务切换动机（深夜护眼 / 偏好人群）。
- **② theme_switch 埋点**：`assets/js/main.js` 切换 handler 内发 GA4 `gtag('event','theme_switch',{from_theme,to_theme})` + 百度统计 `_hmt.push(['_trackEvent','theme','switch',next,1])`（try/catch + feature-detect），把"暗色人群留存更高"假设变可量问题。
- **记住偏好**：localStorage('zx_theme') 既有机制保留（init 脚本 paint 前应用，不跟随系统偏好，与整站一致）。
- **HTML 由 py 生成**：仅改 `assets/.build/build_homeplus.py` 的 dark theme-color（#0D0C0E→#1E1B1F），跑 build 重建 index/engine/gov 三页；`404.html` 非 py 生成，单独对齐内联暗色块。
- **验证**：grep 确认三页 theme-color=#1E1B1F、style.css 朱砂/暖炭、main.js theme_switch 均到位；build exit 0。
- 未执行 git（git 全停令）；预览稿 ui-redesign-proposals.html / dark-mode-proposals.html 可删。

## 引擎频道薄内容治理（独立站 SEO 专家意见，用户拍板 A）
- **SEO 三问结论**：① 路径 `directory/engine/` 不改（此页纯 nofollow 外链工具页、无排名价值，改 slug 成本收益双低且破坏结构一致性）；② 名称"引擎导航"保持（已避通用词"搜索"、与兄弟频道同构、语义诚实）；③ 外访链接**不带**"正协导航"搜索词（零自身 SEO 收益、有操纵痕迹、毁体验；现状已正确=卡片→官网首页 nofollow、搜索框→用户输入词）。
- **薄内容方案 A（采纳）**：保留索引，**给 18 张引擎卡片各补一句独特中文描述**（缓解 thin-content 风险，提供 minimal unique value），不做 noindex。
- **执行**：改写 `assets/.build/_gen_engine.py` 的 `rows` 中每个 `desc`（原偏模板化短句如"中文综合搜索引擎"，扩写为 20–35 字独特说明，如 Google="全球索引量最大的多语综合搜索，学术、地图、翻译等延伸服务齐全"）；重跑 `_gen_engine.py` + `build_homeplus.py` 重生引擎页。
- **验证**：引擎页 18 卡均渲染独特 `card__desc`（旧模板句 0 残留）；JSON-LD 仍 WebPage+ItemList=18；首页 65 卡/无引擎框/无 SearchAction 无回归；全站无 renmin/zhengwu 残留。
- **注**：#2 外访链接带词仍挂起（用户此前说"等看其他 agent 意见"），本轮 SEO 专家亦建议不带，但未最终拍板。
- **圆桌建议字段清单萃取（09:20，用户指令"讨论中agent提议加到页面/卡片的内容都整理出来，看需要哪些字段；数据建表专家xlsx遗失，文档管家重新整理一份md"）**：文档管家执行。核实现状=目录与(32列真值源)均已不存在(真遗失)；仍在(含契约A-P列字段设计)。python扫描39棒聚合每棒建议字段概念，结合#38契约+32列结构，产出(10.8KB)。结构:§0用法+§1页面级+§2卡片级+§3链接级+§4认证级+§5槽位级+§6治理校验层+§7重点候选(圆桌提但32列未收13项:verify_by/region/icp_no/trust_level/page_disclaimer/card_order/publish_date/publisher/doc_no/aka/note/域名白名单/SCHEMA.md)+§8决策汇总+#38契约附录。关键:用户之前删的verify_by(5棒)/region(6棒)/card_order(11棒)恰是圆桌高频提议；icp_no(14棒)/trust_level(13棒)/page_disclaimer(25棒)圆桌共识但32列从未收。临时脚本已删。未重建xlsx(待用户§7勾选授权)。
- **self_links.xlsx 表头统一改造（09:42，用户指令"先对self_links.xlsx的表头进行改造，统一字段名称表述，这样能对比出要增加什么"）**：文档管家执行。现状核实=在用仅3个self_links.xlsx(政协root65行/engine搜索18行/gov人大政府59行)，表头完全一致(27列:站序/分类/type/title/desc/media/tags+link1~10_name/url)，原15个疑似已合并；链接为宽表(每卡最多10链接,仅name+url)。动作=保留原文件,生成统一表头副本到 .workbuddy/staging/self_links_unified/(self_links.{root,engine,gov}.unified.xlsx),表头重命名为对齐候选清单的蛇形命名(row_seq/cat_id/card_layout/card_title/card_desc/card_media/card_tags/link_N_name/url),每表头单元格加批注(中文含义+对应候选字段)。附 README-统一表头与缺口.md:旧→新→候选映射表+缺口分析(页面级全缺/卡片级缺card_id·card_order·verification_*/链接级缺desc·media·tags·source_type·verify_date·verify_channel·verify_by·link_status·review_cycle·region·created_at·updated_at/治理层SCHEMA·域名白名单)+宽表vs长表结构差异+待决策勾选。踩坑:首版3副本同名self_links.unified.xlsx互覆盖→已改按区段命名(root/engine/gov)重跑,删冗余件。本轮仅新增副本+md,未动原self_links/站点/site_data(已遗失)。
- **self_links 扩展列+card_media规则+SCHEMA.md（10:05，用户指令：card_media描述写清维护规则；转换方向对故在self_links补上其他字段；SCHEMA/白名单/SLA谁写）**：文档管家执行。动作=从3原self_links.xlsx重生成统一副本(.workbuddy/staging/self_links_unified/self_links.{root,engine,gov}.unified.xlsx)，原文件未动。①重写card_media批注为8行多识别规则(空值占位/颜色底色/指定文字图标/图片URL/混合分隔/优先级URL>颜色>文字>空值/铁律不存核验)。②补缺口列:卡片级card_id,card_order,verification_type/name/url/desc/enabled(7列)+链接级12字段×10槽(link_N_desc/media/tags/source_type/verify_date/verify_channel/verify_by/link_status/review_cycle/region/created_at/updated_at)=120列；总计27→154列；每列带批注。③起草SCHEMA.md数据字典(字段字典+card_media规则表+认证规则+维护铁律+宽表权衡)。归属答复:SCHEMA.md与完整度SLA属文档管家职责(#39自身提的治理文档三层生命周期+CI读文档校验)，我写不需转介；域名白名单框架我写但哪些域名为官方内容裁决需用户/领域专家定,留空待填。踩坑:批注分支link_N_name误入12字段KeyError→修正分支顺序(先判name/url再判12字段)重跑;旧_transform脚本被_extend取代已删。本轮新增副本+md,未动原self_links/站点。
- **build_homeplus.py vs 4文件结构调研（11:36，用户指令：学习了解4文件结构+用build_homeplus.py创建index.html）**：文档管家调研。现状=4文件在.workbuddy/staging/self_links_unified/(self_links.root.unified 75列65行 + 卡片/链接/页面核验台账各空模板0行)。用户把核验拆成3独立台账，主文件在我上一轮扩展列基础上加了英文蛇形命名(row_seq/cat_id/card_layout/link_N_*/verification_*)。关键错位：build_homeplus.py(assets/.build/)硬编码读assets/xlsx/self_links.xlsx中文27列(站序/分类/type/title/desc/media/tags+link1-10_name/url)，新主文件是英文75列同65卡(仅改名+核验/链接元数据列全空)，且link列名带下划线(link_1_name vs link1_name)；频道页(engine/gov)仍中文格式。直接跑build_homeplus.py会读旧assets/xlsx生成旧index.html,未用新结构。待用户决策接法:①适配生成器双格式表头映射(改build_homeplus.py,根源指向unified,channels保中文)②转回中文27列覆盖assets/xlsx直接跑(丢新列)③仅预览不改生成器。3台账空(0行),生成index.html暂不依赖。
- **build_homeplus.py 双格式适配+生成index.html（11:50，用户选①适配生成器双格式）**：已落地。①备份 assets/xlsx/self_links.xlsx→.bak.xlsx、index.html→.bak(回滚点)；②复制 .workbuddy/staging/self_links_unified/self_links.root.unified.xlsx→assets/xlsx/self_links.xlsx(英文75列,65行)作为根页新数据源(即用户『融合到self_links.xlsx』);③改 build_homeplus.py:加HEADER_NORMALIZE字典(英文蛇形→内部中文键:row_seq→站序/cat_id&cat_name→分类/card_layout→type/card_title→title/card_desc→desc/card_media→media/card_tags→tags/link_N_name→linkN_name/url)+修改load_rows表头归一化+docstring补双格式说明;频道页(engine/gov)中文表头透传不变。④运行生成:根页65卡/engine18卡/gov59卡;新index.html与备份逐字节一致(93528=93528,同65标题/67链接,因数据同65卡仅改名+核验列空)。3台账空(0行)未接入渲染。治理:备份保留待回滚;assets/xlsx/self_links.xlsx现为英文表头唯一真源,staging的unified为草稿副本(编辑需再同步)。未git提交。
- **项目文件命名与存放规范草案（11:58，用户指令：起草文件命名/存放规范+列出需改名移动清单，用户决定）**：文档管家执行。先全仓盘点：5层结构(发布层/资源层/页面层/治理层/暂存层)。产出 。规范要点=§0五层总则(备份不进发布层/临时脚本即删/真值源唯一)+§1目标目录树+§2命名通则(时间戳统一YYYY-MM-DD、-连字符vs_下划线分域、小写snake_case、序号NN-、自解释)+§3数据schema统一(频道页中文27列渐进迁英文75列)+§4备份归档生命周期。附录A违反清单14项(待用户逐条定):A1-P0根目录index.html缺失(仅.bak,需重跑build_homeplus.py恢复,sitemap首URL即/)+A2 index.html.bak滞留发布层/Claw空目录+A3 this.xlsx无意义名/self_links.bak混真值目录/_gen_*.py被取代/link_report·seo_meta_report产物混.build/link-policy.json位置错位/.build隐藏目录+A4 staging两临时脚本滞留/backups命名时间戳不统一(20260825_1130)/trash与backups边界/16号答卷命名特例+A5频道页schema。附录B说明不改项(pages英文vs directory中文并存正确/双build脚本分工/身份文件大写/素材名)。全部只列不改，待用户逐条决定。
- **文件规范附录A逐条执行（12:26，用户拍板：A1已完成/A2清理/A5统一英文75列+py删中文支持/B理解准确/A3-3我决定/A3-4清理/A3-5清理/A3-6收纳/A3-7保留.build修docstring/A3-8我重新判断/A4-9清理/A4-10同意/A4-11同意/A4-12同意）**：文档管家执行。A2删index.html.bak+Claw/；A3-3 this.xlsx→staging/self_links.legacy-8col.xlsx(改名移动)；A3-4删self_links.bak.xlsx；A3-5删_gen_*.py；A3-6建.build/reports/收纳link_report.txt+seo_meta_report.xlsx；A3-7改docstring(link-policy.json→assets/.build/)；A4-9删staging两脚本；A4-10 backups命名统一(2026-08-25-memory-pre-slim/、2026-08-25-npc-xlsx-remove/)；A4-12 16号答卷改名+3处引用同步(归档清单/执行状态报告/结构化汇总)。A5大工程：发现staging engine/gov unified(154列)为上一轮_extend遗留坏数据副本(卡片级全空仅链接有值)→移trash；实际从.bak(中文27列完整数据)转英文75列落盘directory/engine|gov/assets/xlsx/self_links.xlsx，全字段逐行对比零差异；build_homeplus.py删中文表头支持(含中文旧键站序/分类/link1_name即ValueError拒绝)，docstring改单格式；重生成全站根65/engine18/gov59卡；回档点backups/2026-08-27-channels-中文27列/。A3-8实测：GitHub Pages对点目录404(build_homeplus.py=404、.build/=404)→.build无需改名；但assets/xlsx/self_links.xlsx=200可被URL下载(数据源暴露，待用户定)。规范文档加§5执行记录表。踩坑：转换脚本映射方向反(英文列名当键查旧→中文映射)致card_title全空→反转N2O修正；误生成4个0字节空文件(本草案依据等,shell解析md碎片)已清。
- **文件规范融入 CONVENTIONS.md（12:33，用户答复：①xlsx可公开下载没关系不处理；②按规范融入还是新建→判断为融入）**：文档管家执行。回答②=融入（依据草案§2.1+真值源唯一：CONVENTIONS.md是成文约定权威源，再建独立规范=两份并存=本次要治的乱）。执行=在 .workbuddy/docs/CONVENTIONS.md 末尾追加第六章「文件命名与存放规范」(6.1五层总则/6.2命名通则/6.3数据schema一致性-英文75列/6.4备份归档生命周期/6.5目录目标态摘要+URL可访问性实测注记)，纯追加不改动原有5章。草案文件(含附录A执行记录)移入 trash/2026-08-27-文件规范草案-已融入CONVENTIONS/ 保留30天可查。受管文件(CONVENTIONS.md)改动已实时记时间日志(备份铁律)。staging/现仅剩 self_links.legacy-8col.xlsx(待处理) 与 self_links_unified/(root.unified+3台账)。
- **网站全流程运营手册草案（12:50，用户指令：检查站内有无全流程控制表/规定，起草设计-开发-部署-运营-维护全流程手册，明确日/周/月/部署/收录周期节点顺序事项+圆桌优化todo+agent分工+需用户拍板项）**：文档管家执行。检查结论=无全流程控制表，规定碎片化5处（CONVENTIONS§5迭代四阶段/§6命名规范/README§10.4上线清单/SEO技能Backlog/录入SOP），已在草案§0列缺口表。起草产出=.workbuddy/staging/网站全流程运营手册-草案-2026-08-27.md：§0现状盘点+§1六阶段总览+§2节点控制表(建站期/每次部署闸门10步/每次收录7步/每日/每周/每月/每季度/半年年度)+§3圆桌优化Todo(A-F 47项带状态+SEO Backlog+今日未决项)+§4红绿灯(禁止10/不建议5/建议7/必做7)+§5Agent分工表(13类事项对应圆桌角色)+§6需用户拍板10项(D-1手册处置去向/D-2候选字段13项/D-3台账接入/D-4 GA4去留/D-5备案/D-6百度站长/D-7扩省节奏/D-8功能排期/D-9 MEMORY瘦身/D-10 git)+§7纪律衔接(不新增规则只编排时间表)。依据：第1轮圆桌结构化汇总(#16 A-F清单、§5.3待解决)、候选字段清单-圆桌建议§7、zhengxie-seo-standard Backlog、今日此前日志。待用户§6逐条拍板后定稿，处置去向推荐融入CONVENTIONS第七章（未做，等拍板）。
- **全流程手册融入 CONVENTIONS.md 第七章（13:23，用户拍板 D-1=融入）**：文档管家执行。按 12:33 融入第六章同款流程：①在 .workbuddy/docs/CONVENTIONS.md 末尾追加第七章「网站全流程运营手册」(7.0章首说明+现状快照/7.1六阶段总览/7.2节点控制表-建站期7步+部署闸门10步+收录7步+日/周/月/季/半年年度/7.3圆桌Todo A-F分组+SEO Backlog/7.4红绿灯-禁止10不建议5建议7必做7/7.5Agent分工表12类/7.6待用户拍板D-1~D-10-D-1已标✅)，纯追加不改动原有6章；②草案文件移入 trash/2026-08-27-全流程手册草案-已融入CONVENTIONS/ 保留30天可查；③staging/现仅剩 self_links.legacy-8col.xlsx 与 self_links_unified/。受管文件(CONVENTIONS.md)改动已实时记时间日志(备份铁律)。D-2~D-10 仍待用户逐条拍板。
- **文件体检纳入第七章 7.7（13:30，用户确认"文件定期检查整理属于流程手册一部分"，拍板并入 7.7 独立节）**：文档管家执行。回答归属=属于维护阶段核心，已有动作散落(7.2.5每周外链抽查/7.2.6每月台账对账+死链/7.2.7季度域名/7.2.8年度归档+§6命名规范)，缺显式"文件体检"周期线。执行=CONVENTIONS.md 第七章末尾追加 7.7「文件体检」节：分工(主执行文档管家#39/技术协助网站开发#3/数据协助数据建表#38/删除用户把关)+四档周期表(每周10分钟归层核对/每月30分钟全仓盘点+数据对账/staging清点/每季度归档生命周期+备份抽查+表头一致性/半年年度大清理+治理复盘)+红线提醒(删前先记时间日志+批量删先列清单确认+临时脚本即删)。纯追加不改动 7.0-7.6。受管文件改动已实时记时间日志。D-2~D-10 仍待拍板。
- **CONVENTIONS.md 全量重排（13:40，用户质疑"5处碎片化是否证明CONVENTIONS内部无序"，拍板"全量重排+文件章节目录"）**：文档管家执行。先澄清=碎片化是全站范围(CONVENTIONS只占2处)，CONVENTIONS内部按章组织非无序，但章节间确有重叠(backups/trash/MANIFEST三重重复§5.2/§6.4/7.7、顺序按追加时间非主题逻辑、缺目录导航)。执行=①备份回档点 backups/2026-08-27-conventions-重排/(CONVENTIONS.md.重排前+MANIFEST)；②重排为5章主题结构：章首新增「章节目录」导航地图+「重排迁移表」(旧编号→新编号)；一=MEMORY扩展解读(原样不动，MEMORY/skills外部引用兼容)；二=专家转介(原样)；三=页面技术契约(合并原三JSON-LD+原四面包屑)；四=文件治理规范(原六+原五§5.2归档并入4.4+原七§7.7文件体检→4.6)；五=网站全流程运营手册(原七全部+原五§5.1迭代四阶段→5.1)；③去重：backups保留3个/MANIFEST/superseded/trash30天统一§4.4为权威位，7.4必做#1#2改指针；④全内部引用更新(§3/§4→§三、§5.2→§4.4、§6.x→§4.x、§7.x→§5.x)；⑤校验：旧编号仅存于迁移表与来源说明(有意保留)，活跃外部引用(MEMORY↔§1、skills→第一章)全部兼容。新文件533行。受管文件改动已实时记时间日志。回档点保留3个有效备份内。D-2~D-10 仍待拍板。
- **文件职责边界 vs 文件治理规范边界澄清（14:12，用户问"文件治理规范和MEMORY的文件职责边界有没有重复，用不用重新分配"）**：文档管家答复=不重复、不需要重新分配。三处对照：MEMORY「文件职责边界」=治理文档角色分工(元层)、CONVENTIONS §1.8=同内容解释版、CONVENTIONS 第四章=文件系统物理组织(物理层)，两维度互补非重叠。但存在命名撞车(三标题都带"文件")+术语歧义(该节L1/L2=读取分级vs第四章L1-L5=五层目录)。用户拍板"加两行边界指针"。执行=①MEMORY.md「文件职责边界」首行加边界声明(本节=元层角色分工；物理组织见§4；L1/L2=读取分级非目录层)；②CONVENTIONS §1.8 末行加同内容指针。未改标题未挪内容。受管文件(MEMORY.md+CONVENTIONS.md)改动已实时记时间日志。D-2~D-10 仍待拍板。

## 来源指向 UI 方案拍板（产品通，14:27）
- **用户决策**：来源采用**独立 `source_*` 列**（不占 link_1~10 的 10 个动作链接槽），渲染为卡片内单独的「来源」子行（虚线分隔 + 引用图标 + 来源徽标），与主链接区视觉断开；来源默认 `nofollow`。
- **产出**：演示页 `.workbuddy/demo/card-source-row.html`（教育部卡带来源子行 + 百度卡演示"无来源"空态），present_files 实时预览；非发布稿。
- **待办**：是否立项——接 `source_type`/`source_*` 到 build_homeplus.py 渲染 + check_links.py 做 type-aware 差分诊断（含"待人工复核"桶，IP/地域限制不自动删卡）。仍属设想/演示阶段，未改站点/数据源。

## 来源指向落地（14:31，用户拍板"直接改 homeplus.py 和 xlsx，看最终效果"）
- **已实施（用户显式授权改 build_homeplus.py + 3 个 xlsx，故越过首次修改确认）**：
  - `assets/.build/build_homeplus.py`：新增 `SOURCE_GROUP_COUNT=5` + `HEADER_NORMALIZE` 注册 `source_1~5_name/url` → 内部键；新增 `collect_sources(row)` 与 `build_sources(sources)`（无来源返回空串、不渲染、不产生占位行）；`build_card` 末尾注入 `sources_html`。来源链接统一 `target=_blank rel=nofollow noopener noreferrer`（引用非背书）。
  - `assets/css/style.css`：新增 `.card__sources`（grid-column:1/-1 跨整行、虚线分隔 + 来源徽标 + 引用图标、type1 不内缩 / type2·3 对齐 14px 内缩）。
  - 3 个 `self_links.xlsx`：追加 `source_1~5_name/url` 共 10 列（列数 75→85）。根页前 5 卡 + 政务页前 3 卡填了**示例来源**（百度百科 / 中国政府网，属占位待替换）；引擎页仅加列无数据。
  - 跑 build 重生 index.html + directory/engine + directory/gov（65/18/59 卡），首页实渲染 5 个来源子行、6 条来源链接（全国政协 2 条）。本地 `http.server 8123` 预览已开。
- **备份**：`.workbuddy/backups/2026-08-27-source-col/`（build_homeplus.py/style.css/3 个 xlsx 各 .bak）。
- **未做（"到时完善"范围）**：① check_links.py 仍未做 type-aware 差分诊断（来源×目标可达性对照、失败模式标签、"待人工复核"桶）；② 示例来源为占位，需联网核实替换为真权威出处；③ JSON-LD / SEO 未动来源；④ 未 git 提交。

## verification_* 列删除 + 错填来源转存（14:53，用户拍板"删除，转"）
- **决策依据**：`verification_*` 为圆桌萃取遗留**未实现 schema 桩**——build 从不渲染（仅 docstring 提及"留待后续"）、check_links.py 完全不读、全仓除 docstring 外无代码依赖、生成 HTML 零命中。来源（source_*）已承担"权威出处=信任信号"角色，二者在本站实际语义重叠。故删。
- **数据实情**：gov(59)/engine(18) 零数据；root(65) 仅 2 行有值且**填错列**——`verification_type` 写"机构简介/机构介绍"、`verification_name` 写 URL（12377.cn / 12321.cn），本质就是出处链接，本该进 source_*。
- **执行（3 个 self_links.xlsx + build_homeplus.py，用户显式授权删+转，越过首次修改确认）**：
  - 删 `verification_type/name/url/desc/enabled`（col 11–15）五列，三表均 85→80 列；删除按"从右到左"保索引有效。
  - **转**：root 那 2 行（row_seq 9023「中央网信办…举报中心」、9024「12321…举报受理中心」）的错填链接 → 对应卡 `source_1_name/url`（type 作来源名、name 字段的 URL 作来源 URL）；两张卡此前 source 全空，无冲突。
  - `build_homeplus.py` docstring：删除 verification 列清单行 + 行内 `verification_*/` 与"核验"字样（保留 link_N_* 留待徽章的注记）。
  - 重跑 build：根 65 / engine 18 / gov 59 卡正常；首页精确 `card__sources` 块 = 7（7 张卡有来源）、来源链接 11（政协 5 + 其余 6 各 1）；全网 HTML 零 `verification` 残留。
- **验证**：grep `<div class="card__sources">`=7 与数据层 7 张吻合（此前"14"是 `card__sources` 同时命中 `card__sources-label` 的误判，已排除非 bug）；两张转存卡来源行正确显示 12377/12321 链接。
- **备份**：`.workbuddy/backups/2026-08-27-verify-del/`（build_homeplus.py/style.css/3 个 xlsx 各 .bak，删列前快照）。
- **未做**：来源域名派生"官方来源"徽标（替代原 verification 概念，省手填）仍待排期；check_links.py type-aware 诊断未做；未 git 提交。

## 来源行超出横向滑动（15:03，用户指令"来源行也像标签行、链接行一样超出滑动"）
- **根因**：`.card__sources` 原为 `flex-wrap: wrap`（多则换行），而标签行/链接行是 `overflow-x:auto + white-space:nowrap + flex:none` + JS `is-scrollable` 高亮/滚轮接管/触摸激活。
- **执行（改 `assets/css/style.css` + `assets/js/main.js`，用户显式授权）**：
  - CSS：`.card__sources` 并入 overflow-x 滚动组（line 117）、scrollbar 隐藏组（line 125）、4 处 `.is-scrollable` 规则（grab/active/hover/touch-active）+ 暗色描边规则均补 `.card__sources`；`.card__sources` 的 `flex-wrap: wrap`→`nowrap`，`.card__sources-label` 与 `.card__source` 加 `flex: none`（不收缩、不换行）。
  - JS：`SCROLL_ROW_SEL` 增加 `.card__sources` → 溢出检测（`scrollWidth > clientWidth`）、悬停滚轮接管为左右滑、触摸激活高亮，全部自动覆盖来源行。
  - 虚线分隔（`border-top`）+ 来源徽标在滚动容器内固定不动，仅标签与链接随内容横滑；无溢出时不标 is-scrollable、不劫持滚轮。
- **验证**：CSS 括号平衡 266/266；HTML 直引 assets/css/style.css + assets/js/main.js（非 build 副本，改即生效，刷新即见）；政协卡 5 条来源在窄卡宽下将触发横滑。
- **备份**：`.workbuddy/backups/2026-08-27-source-scroll/`（style.css + main.js）。
- **未做/待定**：未 git 提交。

## 来源标签 sticky 固定 + 空卡占位保持等高（15:12，用户指令"固定；没有来源也要占行保等高"）
- **需求拆解**：①「来源」标签在横滑时固定不动（sticky）；② 没有来源的卡也渲染占位行、预留行高，保证网格内卡片等高、来源行垂直位置对齐。
- **「来源是否显示」决策（PM 答复）**：空卡**不显示**「来源」标签（保持静默占位），理由=root/gov/engine 多数卡暂无来源，显示"来源 暂无"会成 50+ 处噪声；行高仍占位即可保等高。若用户要"治理信号"式常显"来源 暂无"可一行翻转——本次未做。
- **执行（改 `assets/css/style.css` + `assets/.build/build_homeplus.py`，用户显式授权）**：
  - CSS：`.card__sources` 加 `min-height:27px`（预留行高）；新增 `.card__sources--empty`（去虚线 `border-top:none` + `min-height:14px`，避免空白卡出现孤立分隔线）；`.card__sources-label` 加 `position:sticky; left:0; z-index:2; background:var(--bg-warm); padding-right:8px; margin-right:-2px; box-shadow:7px 0 6px -4px var(--bg-warm)`（横滑时标签固定、用卡片暖底遮挡滑过的链接、右侧柔和淡出）。
  - build：`build_sources()` 无来源时返回 `<div class="card__sources card__sources--empty"></div>`（占位空行）而非空串；有来源照旧渲染标签+链接；同步更新两处 docstring。
  - main.js 无需改（`SCROLL_ROW_SEL` 已含 `.card__sources`，空行无溢出不触发 is-scrollable）。
- **验证**：重跑 build 成功（root 65/engine 18/gov 59 卡）；root 页 `card__sources--empty`=58 + 有来源 `card__sources`=7 合计 65，与数据层吻合；sticky 规则命中 1；CSS 括号平衡 267/267。
- **备份**：`.workbuddy/backups/2026-08-27-source-fixed/`（style.css + build_homeplus.py）。
- **未做**：未 git 提交。

## 来源行等高修复：空行也要虚线（15:21，用户指令"同排有/无来源高度不一，全加虚线"）
- **根因定位**：卡片网格容器 `align-items: start`（style.css:438），**不拉伸**卡片高度，每张卡按自身内容高排。此前空态行 `min-height:14px + 无虚线`，有来源行 `27px + 1px虚线`，差约 8px+ → 同排参差。虚线仅差 1px，主因是空/满行 min-height 与 padding 不等。
- **执行（改 `assets/css/style.css` + `assets/.build/build_homeplus.py`，用户显式授权）**：删 `.card__sources--empty` 覆盖规则（去 `border-top:none`/`padding:6`/`min-height:14`），空行改用与有来源行**完全相同**的 `.card__sources` 基础样式（同样 `border-top:1px dashed` + `min-height:27px`）→ 像素级等高；build 空态输出由 `card__sources card__sources--empty` 改为纯 `card__sources`。
- **验证**：重跑 build；root 页 `card__sources`=65（含空行全带虚线）、`--empty` 残留 0；虚线规则仅 base 命中 1 处；CSS 括号平衡 266/266。
- **注**：虚线作统一脚部分隔线，非空噪声；若后续想去掉空卡虚线需同步接受高度差（勿回退）。
- **未做**：未 git 提交。
- **注意**：来源链接现为演示数据，正式发布前须替换为真实权威来源并核实可达。

## 全站 UI 重设计方案（设计架构师 Diana，15:41）
- **背景**：用户授权以 UI 权威身份重做全站 UI（亮+暗双模），无限制，hover/阴影/闪动等交互哲学由我定；要求继承第1轮圆桌主题色共识。
- **圆桌共识落地**：① 红金白"奢华尊贵"为第一眼差异化资产须继承；② 吸收 UI 专家#5 七项修复——亮色焦点环换深金 #9E7E36(WCAG 2.4.7)、广告区金30%分隔线+"广告"徽章区隔、暗色描边柔和化、.card__link 直角统一圆角；③ 动效克制(减法优先/可信优于流量)，无闪动/无金光扫。
- **产出**：可切换实机预览 `ui-redesign-proposals.html`（4 方案 × 亮/暗双模，真实版式复刻 + 推荐分级面板）。4 方案：
  - A 朱玺政务 ★★★（当前红金白精修：红→朱红 #C0392B、暗→暖炭 #1E1B1F/朱砂 #E2604E；最推荐，零品牌损耗+修 a11y+最低风险）
  - B 翰墨丹青 ★★☆（宣纸/墨青底+青瓷绿交互 #3E7C6E+朱砂点缀；书卷气，回应年轻用户"正式但不土"）
  - C 鎏金现代 ★★☆（纯白大留白+无衬线标题+鲜红 #E23B2E；拉近年轻，弱官气）
  - D 赤金典藏 ★☆☆（奶咖/深酒红/富金极致奢华；土味风险最高，仅备选）
- **交互哲学（我定）**：悬停=抬升(var(--lift))+描边提亮+柔影；入场=淡入上浮(尊重 prefers-reduced-motion)；无闪动/无金光扫(伤信任+a11y)。
- **状态**：设计预览稿，非站点文件。待用户拍板采用哪套 → 写入 style.css 的 :root 与 [data-theme="dark"] 并同步 index.html theme-color；改文件前需用户确认（治理铁律）。
