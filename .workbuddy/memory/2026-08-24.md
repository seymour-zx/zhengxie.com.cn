# 2026-08-24

## 身份文件跨设备同步方案（已落地）

- 把 4 个身份文件（SOUL.md / IDENTITY.md / USER.md / BOOTSTRAP.md）从用户主目录 `C:\Users\seymo\.workbuddy\` 移动到工作空间 `D:\Universal Space\zhengxie.com.cn\.workbuddy\`，随 Git 仓库同步。
- 在用户主目录 `C:\Users\seymo\.workbuddy\` 创建 4 条**文件符号链接（symlink）**指向工作空间里的真实文件（C:→D: 软链接，靠 Windows 开发人员模式实现；普通 `mklink` 经 cmd 调用失败，最终用 Git Bash 的 `ln -s` 成功）。
- 应用仍从用户主目录路径读取身份，无感；真实文件在同步区，跨设备一致。
- **关键约定**：符号链接是本地文件系统对象，Git 不跟随同步。换设备后，需在**那台设备**的用户主目录同样建 4 条软链接指向同步下来的工作空间副本（每台设备各建一次链接）。
- 注意：用户主目录的 `C:\Users\seymo\.workbuddy\` 下还有大量**未同步**内容（用户级技能、workbuddy.db 自动化、mcp.json、受管 node/python），这些**不要**用符号链接/同步塞进工作空间（有泄密+臃肿风险）。只链接这 4 个身份文件。
- 开发者模式下 `cmd //c mklink` 在本环境无法正常传参（只弹 cmd 横幅），跨卷软链接改用 Git Bash `ln -s "$DST/$f" "$SRC/$f"` 更稳。

## git 状态

- 仓库根：`D:\Universal Space\zhengxie.com.cn`（含 .git）。
- 这 4 个文件当前为未跟踪（untracked），未被 .gitignore 忽略 → `git add .workbuddy/SOUL.md .workbuddy/IDENTITY.md .workbuddy/USER.md .workbuddy/BOOTSTRAP.md` 后提交即可同步。

## 后续（同日）
- 用户已 `git commit && push`：身份文件 4 份 + `MEMORY.md` 已同步到远端。
- 跨设备策略三件套全部定稿（技能项目级 / 记忆工作空间级 / 身份已同步）；写入 MEMORY.md 长期约定。
- 用户要求：换设备开场第一时间提醒其重建身份软链接 → 已在 MEMORY.md 顶部加「🔔 开场必做」最高优先级指令。
- md 文件整合计划已拟（用户自行执行）：`assets/skills/SKILL.md` + `IMAGE_OPTIMIZATION.md` 移入 `.workbuddy/skills/`；新建 `.workbuddy/docs/CONVENTIONS.md` 去重「跨设备权威源约定」「专家转介纪律」；README 瘦身、去掉重复段；改 `build_homeplus.py:62` 过时注释；清理 SEO 技能里的悬空引用。

## 文件职责边界约定（已写入 MEMORY.md）
- 用户确认在 MEMORY.md 增加「文件职责边界」一节，明确三者分工：
  - **MEMORY.md** = agent 操作记忆/硬规则（跨设备策略、身份软链、开场提醒、背景、安全边界、铁律）。
  - **CONVENTIONS.md**（计划中，`.workbuddy/docs/`） = 项目成文约定单一权威源（规定本身），从 README/SKILL 去重而来。
  - **技能 SKILL.md** = 领域操作 SOP（怎么做）。
  - 三者都同步；都讲"跨设备"但角度不同：MEMORY 讲"存哪级"、CONVENTIONS 讲"哪些 md 可读"、技能讲"操作步骤"。互补不重复。

## md 整合执行（已落地，未提交）
- 按用户批准的计划完成 md 重组（用户授权"按计划开始整理"）：
  - `git mv`：`assets/skills/SKILL.md` → `.workbuddy/skills/zhengxie-subpage-sop/SKILL.md`；`assets/skills/IMAGE_OPTIMIZATION.md` → 同目录；删空 `assets/skills/`。
  - 新建 `.workbuddy/docs/CONVENTIONS.md`：收敛「跨设备权威源约定」+「专家转介纪律」为单一权威源（去重 README 与 SKILL 两处重复）。
  - 改写 zhengxie-subpage-sop/SKILL.md：重写"存放说明"（现为已注册项目技能）、删顶部两处重复约定块改指针、图片规范路径改同目录、删文末专家转介纪律全文改指针。
  - 瘦身 README.md：跨设备约定段改写指向 CONVENTIONS、所有 `assets/skills/...` 路径引用更新、§3.1 专家转介纪律改指针、目录树移除 assets/skills 并补 .workbuddy 段。
  - 清理 SEO 技能悬空引用：删除对 `zhengxie_seo_review.md`/`zhengxie_vertical_plan.md`/`docs/BAIDU_SEO_STRATEGY.md` 的引用（声明已弃用不依赖）。
  - 改 `assets/.build/build_homeplus.py` docstring 注释：跨设备权威源改为 README + .workbuddy/skills/* + .workbuddy/docs/*。
  - 修正 IMAGE_OPTIMIZATION.md 头部"非权威源"旧说明。
- 验证：全仓 grep 确认无 `assets/skills/` 残留路径；仅 `zhengxie_vertical_plan.md` 自身仍引用 `zhengxie_seo_review.md`（两文件为仓库根目录的孤儿底稿，未被任何文件引用，留待用户决定保留/删除）。
- 按 Git 纪律：本次仅改动文件、**未提交/推送**；改动清单见 `git status`。

## Git 提交/推送纪律（已写入 MEMORY.md）
- 用户设定：我可以**提醒**有文件未提交/推送，但**绝不主动询问**"要我提交吗"。
- 若用户要求提交/推送 → 必须**二次确认**（复述文件清单+目标仓库）后才执行那一次。
- 纪律长期有效，除非用户明确更改；用户临时授权某次提交 = **一次性有效，不等于改纪律**。
- 所有 git commit/push 默认由用户自己完成。

## 孤儿底稿收尾（用户选 A，已落地，未提交）
- 用户确认：`zhengxie_seo_review.md` 与 `zhengxie_vertical_plan.md` 两份根目录孤儿底稿的处理。
- 判定：`zhengxie_seo_review.md`（P0/P1/P2 评审）≈95% 已被 SEO 技能 Known issues/backlog + LOCKED/Decided 覆盖 → 直接删，无需合并。
- `zhengxie_vertical_plan.md`（垂类方案）≈85% 已覆盖，但有 2 块技能缺失的干货已并入 SEO 技能「参考草稿（未实施）」段：① 具体 meta 建议值（title/description/keywords 草稿）；② dofollow TRUSTED 档实施线索（link_attr 474–505 行、TRUSTED_ATTR、名单）。
- 已执行：`rm` 删除这两份文件；根目录现仅剩 `README.md`，全仓再无 `zhengxie_*.md`/`*_plan.md`。
- 按 Git 纪律：未提交/推送；改动随其余 md 重组一起待用户提交。

## Claw 空目录清理（已删除）
- 用户问 `Claw/` 是什么。核查：完全空目录、git 未追踪、无历史、全仓零引用、今天 02:43 凭空出现，无意义。
- 已 `rm -rf Claw` 删除；根目录结构正常。该目录不在 git 中，删除不影响提交记录。
