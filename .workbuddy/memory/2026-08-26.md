# 2026-08-26 工作日志

## 频道结构收敛落地（续前序决策）
前序已拍板：①政协/民主党派不单独建频道、聚流给首页；②C 类（国际政治/外国政务）全砍；③AI 降级辅助层；④坚持建 2 个独立频道（人大、政务机构）；⑤数据来源=联网查官方站核实，不编造 URL。

### 本轮完成
- 新建 `directory/renmin/`（人大频道）：`self_links.xlsx`(12 站) + `self_meta.json`(含 channel_intro) + build 生成 `index.html`(12 卡片)。
  - 收录：全国人大 + 北京/上海/天津/重庆(直辖市) + 广东/江苏/浙江/山东/四川/湖北/河南(省)。全部 .gov.cn 官方域，经 WebSearch 核实。
- 新建 `directory/zhengwu/`（政务机构频道）：`self_links.xlsx`(13 站) + `self_meta.json` + `index.html`(13 卡片)。
  - 收录：国务院(www.gov.cn) + 国家政务服务平台 + 发改委/教育部/财政部/公安部/民政部/司法部/人社部/自然资源部/生态环境部/交通运输部/工信部。
- 手写 `directory/index.html` 导航 hub（门户页，非生成器产出）：列 2 频道，含 WebPage+ItemList+ BreadcrumbList JSON-LD，沿用站点红/金主题与 footer。
- `sitemap.xml`：删除 14 个幻影频道（ai/bank/insurance/stock/media/nav/gov/npc/party/world-*），增补 renmin/zhengwu（priority 0.65），保留 hub。现 13 条 URL。
- 全站唯一 `/directory/` 引用为 footer「频道导航」→ 现已解析到 hub（不再 404）。

### 关键修复
- 生成器 `_gen_channels.py` 初版 HEADER 列序错误（所有 name 在前、所有 url 在后），导致 `collect_links` 取错列、全部卡片外链与 JSON-LD ItemList 为空。已改为 name/url 交错（link1_name,link1_url,…），重生成后链接与 ItemList(12/13) 正常。

### 备注
- 人大频道仅收录已核实的 12 省级/直辖市+全国；其余省份（含设区的市人大）待用户确认是否扩展（需逐一核实官方域）。
- 临时生成脚本保留于 `assets/.build/_gen_channels.py`（频道 xlsx 数据源的引导脚本，非 build 管线一部分）。
- 未执行任何 git 操作（遵守 git 全停令）；改动文件待用户本地提交。

## 政务导航全量扩展落地（Task #15/#16/#17）
- 架构漂移修正：原 `directory/renmin/`、`directory/zhengwu/` 源 xlsx 已不存在，`_gen_gov.py` 失效。将 gov 频道数据源收敛为单一 `directory/gov/assets/xlsx/self_links.xlsx`，并把 `_gen_gov.py` 重写为**自包含全量生成器**（仿 `_gen_engine.py`），内嵌全部卡片数据，不再依赖已消失的源。
- Task #15 完成：31 个省级人大官方域全部联网核实（前序 8 + 本轮 12 + 既有 11 直辖市/省，补：河北 hbrd、山西 sxpc、辽宁 lnrd、吉林 jlrd、黑龙江 hljrd、安徽 ahrd、福建 fjrd、江西 jxrd、湖南 hnrd、海南 hainanpc、贵州 gzrd、云南 ynrd、陕西 sxrd、甘肃 gsrdw、青海 qhrd、内蒙古 nmgrd、广西 gxrd、西藏 xizangrd、宁夏 nxrd、新疆 xjpcsc）。
- Task #16 完成：26 个国务院组成部门官方域全部核实。新增收录 外交部 mfa / 国防部 mod / 科技部 most / 国家民委 neac / 住建部 mohurd / 水利部 mwr / 农业农村部 moa（注意非 mara）/ 商务部 mofcom / 文旅部 mct / 国家卫健委 nhc / 退役军人事务部 mva / 应急管理部 mem / 中国人民银行 pbc / 审计署 audit 共 14 个。**国家安全部无公开官方门户（仅 12339 举报平台），按合规铁律跳过不收录。**
- Task #17 完成：重跑 `build_homeplus.py`，`directory/gov/index.html` 现 **59 卡片**（人大 32：全国1+直辖市4+省22+自治区5；政务 27：国务院1+国家平台1+组成部门25）。分类按钮动态生成：全国/直辖市/省/自治区/国务院/国家平台/组成部门。JSON-LD：WebPage+isPartOf WebSite+mainEntity(ItemList numberOfItems=59)。外访链接均 https 官方域、`.gov.cn` dofollow。

### 备注
- 沙箱 Windows 回收站不可用 → build 预删旧 index.html 失败（safe-delete fail-closed）；新内容已覆盖写，结果正确。本地有回收站时正常。
- 未动 `sitemap.xml`（build 不自动维护，待用户授权）。
- 未执行 git（git 全停令）；改动文件：`assets/.build/_gen_gov.py`(重写)、`directory/gov/index.html`(重建)、`directory/gov/assets/xlsx/self_links.xlsx`(重建)。待用户本地提交。
