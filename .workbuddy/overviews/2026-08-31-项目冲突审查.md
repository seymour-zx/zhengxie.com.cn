# zhengxie.com.cn 全项目冲突 / 互斥审查（2026-08-31）

> 审查范围：公开层（index.html / pages / topics / README / robots / sitemap / ads / assets）+ 私有治理层（宪法 / contracts / memory / MEMORY.md）+ 构建真值源（assets/xlsx）
> 方法：静态读取 + xlsx 真值解析 + 坐标扫描 + CSS/结构铁律核对
> 结论：**数据层完全自洽；原发现 1 项 P0、2 项 P1、4 项 P2、1 项 P3。截至 2026-08-31 二次拍板，P0/P1/③⑤⑨ 已回改解决，⑦ 经复核为误报已撤回；首页/政务/搜索三页广告保留不清。**

---

## 〇、回改与状态更新（2026-08-31 造物主拍板）

| 原项 | 拍板 | 处置 | 状态 |
|---|---|---|---|
| ① P0 契约03 §四+§八 广告双策略互斥 | 授权 | 已回改为「同池自然参与、取消首次必出/比例」 | ✅ 已解决 |
| ② P1 契约03 行数「一行」滞后 | 随①一并授权 | 已改为「2 行 / RANDOM_LINES=2」 | ✅ 已解决 |
| ③ P1 宪法 §11.5 死链路径 | 授权 | 已改为 `D:\Universal Space\zhengxie.com.cn\.workbuddy\` | ✅ 已解决 |
| ⑤ P2 MEMORY「ZX 已删」歧义 | 确认 | 已澄清：`ZX`=被删技术组件≠`zx_`前缀；`zx_`=正协拼音保留 | ✅ 已解决 |
| ⑦ P2 hub 广告位 | 答复「已删」 | **复核为误报**：hub `ad_slots` 本就为空，HTML 零广告代码 | ⚪ 撤回 |
| ⑨ 契约04:42 残留旧策略 | 授权 | 回改为「同池自然参与、无首次必出/比例」 | ✅ 已解决 |

---

## 一、P0 · 严重（违反宪法铁律1「单一权威，永不两处说法不同」）

### ① 随机漫步「广告策略」——契约03 与 契约02 + 实现 直接互斥
| 来源 | 说法 |
|---|---|
| **契约03 §四**（构建·第4步定稿）+ §八决策记录 | ~~「广告双策略：随机池=普通池+广告池(is_ad=True)；首次必出 1 张广告卡 + 每批固定比例」「✅ 随机漫步改造 + 广告双策略参数放 build 常量」~~ **（2026-08-31 已回改）** |
| **契约02 §一**（数据模型·is_ad 字段，2026-08-30 拍板） | 「随机漫步与普通卡**同池自然参与（不分池，广告池/首次必出/比例策略已取消，2026-08-30 造物主拍板）**」 |
| **main.js 实际实现** | 「广告卡同池参与，不分普通/广告」「随机取 n 行，按数量加权随机类型」——无首次必出、无比例、同池 |

**判定**：契约03 §四/§八原描述「已取消」旧方案，与契约02 §一、main.js 实装正面矛盾。实现方向正确，但契约03 未回改。
**修复（2026-08-31 已执行）**：契约03 §四改为「广告卡与普通卡**同池自然参与**（不分池）；广告池/首次必出/比例策略已取消（详见契约02 §一）」；§八决策记录同步改为「每批 2 行 + 同池自然参与、取消首次必出/比例」。

---

## 二、P1 · 较重要（文档滞后 / 路径错误）

### ② 随机漫步「展示行数」互斥
- 契约03 §四原写「随机展示**一行/一页**卡片」；README §七、main.js（`RANDOM_LINES = 2`）、MEMORY 均写「**2 行**」。
- **修复（2026-08-31 已执行）**：随①一并回改为「2 行/一页，RANDOM_LINES=2」。

### ③ 宪法 §11.5 软链接示例路径错误（照抄会失败）
- 原：`New-Item ... -Target "D:\Universal Space\miniworld\.workbuddy\$f.md"`（miniworld 为早期代号，目录不存在 → 悬空死链）。
- **修复（2026-08-31 已执行）**：改为 `D:\Universal Space\zhengxie.com.cn\.workbuddy\$f.md`（与真实工作目录、CNAME/README 域名一致）。

---

## 三、P2 · 建议修正 / 需澄清

### ④ pages.unified.xlsx 名不副实（范围误导）
- 该表仅含 4 个 dir_path（`/`、`topics/gov`、`topics/search`、`topics`），**不含 9 个手写说明页**（pages/about 等）。
- 实际页面全貌分散：pages.unified（4 拼装页）+ 手写 pages/*（9 页）+ sitemap（手工列全 13 页）。
- 建议：在契约02 §二或表头注释说明「本表仅含拼装生成的专题页；pages/* 说明页为手写，不入本表」。

### ⑤ MEMORY「ZX 已删」vs 代码 zx_ 前缀（已澄清）
- 原 MEMORY：「ZX 已删（2026-08-30）；后端+事件分析迁移时提醒」与代码 `zx_theme`/`zx_favs`/`zx_notice_closed` 等字面矛盾。
- **澄清（2026-08-31 造物主确认）**：`ZX` 指某**已删技术组件**（后端/事件分析），与 `zx_` 前缀无关；`zx_` = 「正协」拼音缩写，作 localStorage/统计 key 命名惯例，**保留**。
- **修复（已执行）**：MEMORY 第 47 行改为「ZX(被删技术组件，≠`zx_`前缀)；`zx_`=正协拼音前缀，保留；后端+事件分析迁移时提醒」。

### ⑥ main.js 公开层注释泄漏内部脚本名（轻微）
- main.js（公开层 assets/js/）注释提及 `build_homeplus.py` 具体构建脚本名。
- 不违反契约06（契约06 约束 README），但属轻微信息暴露。建议改注释为「由构建流程生成」。

### ⑦ ~~topics hub 页 ad_slots=1,2~~（**误报，已撤回**）
- 原报告称 hub 页 `ad_slots=1,2` 会注入广告。
- **复核（2026-08-31，xlsx 真值解析）**：hub 行 `dir_path='topics'` 的 `ad_slots=''`（**空**）；构建产物 `topics/index.html` 经 grep 确认**零广告代码**（无 `adsbygoogle`/`ad--top`/`data-ad-slot`）。
- 结论：hub **从未配置广告**，原报告为误报。造物主答复「已删」在 hub 上本就成立，无需改动。
- **仍带广告的真实页面（保留不清）**：首页 `index.html`、`topics/gov`、`topics/search` 三页 `ad_slots=1,2` 且构建产物有广告代码（xlsx 驱动）。**造物主 2026-08-31 拍板：这三页广告保留，不清掉**（故 `pages.unified.xlsx` 不动）。

### ⑨ 契约04-校验.md:42 残留旧广告策略（已解决）
- 契约04 §四 L4 浏览器清单第 42 行：「- [ ] 随机漫步：随机展示不跳转、换一批/退出、**广告注入（首次必出 + 比例）**」。
- 该描述与已回改的契约02 §一 / 契约03 §四 **正面矛盾**（新决策：同池自然参与、取消首次必出/比例）。
- 属与①同类的文档互斥，上轮比对时未覆盖契约04，故为本次新发现。
- **已执行（2026-08-31 造物主授权）**：已回改为「广告卡同池自然参与（不分池，无首次必出/比例）」。

---

## 四、P3 · 文案轻微

### ⑧ README 第 5 行「数千张词条卡片」
- 实际 cards.unified 共 **463 张**（首页337+政务59+搜索67），为「数百」非「数千」。
- 建议：改为「数百张」或「400+ 张」。

---

## 五、已核查一致项（无冲突，供安心）

| 核查项 | 结果 |
|---|---|
| sitemap.xml 13 个 URL ↔ 实际页面 | ✅ 1:1 对应 |
| robots.txt 放行 css/js/images | ✅ 与 README §八一致 |
| 公开层 GitHub 坐标泄露 | ✅ 零真实坐标（占位符，符合宪法 §10.2） |
| style.css `scroll-behavior:smooth` | ✅ 已移除，符合铁律 |
| style.css 暗色变量 | ✅ `#1E1B1F/#2A252A/#D9B978/#ECE6DF` 与 README 一致 |
| index.html 结构 ID | ✅ `consent-bar`/`category-bar`/`cards-container`/`scroll-btns` 齐全 |
| 首页分类边界 | ✅ 全属政协体系，无越界 |
| cards 真值源 | ✅ 463=337+59+67；is_ad 全空；dir_path 无未知值 |
| gov 含「人大」类 / search 分类 | ✅ 与契约02 §3.2/§3.3 一致 |
| CNAME | ✅ `zhengxie.com.cn` 一致 |
| 首页集合搜索框 | ✅ index.html 无 `data-engine`（search_box 空） |
| 主题/localStorage key 命名 | ✅ README 与 main.js 一致（zx_theme/zx_favs/zx_notice_closed） |
| README 目录树 | ✅ 不列 assets/xlsx、assets/.build |
| README 卡数声明（59/67） | ✅ 与 xlsx 一致 |

---

## 六、建议拍板事项（更新）

1. ✅ ① P0 契约03 §四+§八 已回改（授权）
2. ✅ ⑤ ZX 指代已澄清（确认）
3. ✅ ③ 宪法 §11.5 路径已修正（授权）
4. ✅ ⑦ hub 广告位 = 误报撤回（hub 本无广告）；首页/政务/搜索三页广告**保留不清**（造物主拍板）
5. ✅ ⑨ 契约04-校验.md:42 已回改（授权，2026-08-31）：「同池自然参与、无首次必出/比例」
6. ⚪ ④ pages.unified 范围说明、⑥ main.js 注释、⑧ README「数千张」 —— 可选优化，不阻塞
