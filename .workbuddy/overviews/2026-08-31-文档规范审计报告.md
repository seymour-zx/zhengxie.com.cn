# 文档规范审计报告（2026-08-31 10:29）

> 审计范围：公开层（根 + assets / topics / pages / directory / Claw）+ 内部治理层 .workbuddy
> 依据：世界坐标保护（防泄漏第一）、README 铁律（契约 06）、公开分层铁律、文件治理铁律、N-03（废弃 directory → topics）
> 性质：**检查型审计**，未改动任何文件；修复需造物主拍板

## 一、核心结论

| 等级 | 项 | 数量 | 状态 |
|---|---|---|---|
| **P0** | 公开层泄露部署平台（GitHub Pages 字样） | 10 处 | 需改写 |
| **P1** | 公开真实邮箱 | 1 处 | 需拍板 |
| **P1** | 旧世界 `directory/` 残壳 | 1 处 | 需拍板删除 |
| **P1** | 异常空目录 `Claw/` | 1 处 | 需拍板删除 |
| **P2** | README 部署章 GitHub 字样（占位） | 边界合规 | 视需要淡化 |
| **P2** | assets 内部文件公开部署 | 既定架构 | 提示 |
| **P2** | 统计/广告真实 ID 嵌入公开页 | 既定决策 | 提示（迁境内时关） |
| ✅ 通过 | README 契约 06 校准 8 项 / 内部 md 治理 / pages 一致性 / robots 隔离 | — | 合规 |

## 二、P0 — 公开层泄露部署平台（GitHub Pages）

**违规点**：世界坐标保护（防泄漏第一）"公开层禁止 github.com 用户路径/平台信息"。当前公开层多处写明"当前项目托管于 GitHub Pages"，可反推 github 用户，与 MEMORY「公开层零 GitHub 坐标泄露」目标冲突。

**清单（10 处）**：

| 文件 | 行 | 形式 | 严重度 |
|---|---|---|---|
| `pages/about/index.html` | 91 | **可见正文**："当前托管于 GitHub Pages（境外服务器），处于静默测试阶段" | 🔴 最高（用户可直接看到） |
| `pages/about/index.html` | 171 | HTML 注释 | 🟠 |
| `pages/changelog/index.html` | 77 | **可见正文**（li 项："备案号以注释占位（GitHub Pages 托管，暂无 ICP 备案）"） | 🔴 高 |
| `pages/changelog/index.html` | 141 | HTML 注释 | 🟠 |
| `pages/disclaimer/index.html` | 151 | HTML 注释 | 🟠 |
| `pages/contact/index.html` | 138 | HTML 注释 | 🟠 |
| `pages/guide/index.html` | 153 | HTML 注释 | 🟠 |
| `pages/submit/index.html` | 168 | HTML 注释 | 🟠 |
| `pages/overview/index.html` | 214 | HTML 注释 | 🟠 |
| `pages/sitemap/index.html` | 154 | HTML 注释 | 🟠 |
| `pages/privacy/index.html` | 196 | HTML 注释 | 🟠 |

**建议修复**：统一改写为纯备案号占位，不提任何托管平台。例如：
`<!-- 备案号占位：完成 ICP 备案后替换粤ICP备XXXXXXXX号并取消本注释 -->`
（`about` / `changelog` 的可见正文同理改为中性表述，去掉"GitHub Pages / 境外服务器 / 静默测试"等平台与阶段自述。）

## 三、P1 — 应处理项

### P1-1 公开真实邮箱
- `pages/contact/index.html:84`：`seymour.zx@foxmail.com`（真实邮箱，对用户可见）
- 坐标保护铁律未明文禁邮箱，但此邮箱与 github 用户 `seymo` 关联，属个人真实联系方式。
- **待拍板**：①保留真实邮箱 / ②改用站内表单（不暴露邮箱）/ ③改为占位（如 `联系邮箱见页脚表单`）。

### P1-2 旧世界 `directory/` 残壳
- 路径：`directory/engine/assets/{json(空), xlsx(空)}`、`directory/gov/assets/(空)`
- 性质：旧世界废弃目录。N-03 已拍板"专题统一目录 = topics（废弃 directory）"，消融台账记"`directory/` 留到最后由造物主删壳"。
- 当前为**空目录残留**（git 不跟踪空目录，属本地/部署疏漏）。
- **待拍板**：删除 `directory/` 整棵（破坏性，需造物主确认）。

### P1-3 异常空目录 `Claw/`
- 路径：根目录 `Claw/`（仅含 `.` / `..`，无内容）
- 性质：2026-08-31 代码审查报告曾标记"P2-1 Claw/ 空目录残留 → 已删除"，但当前文件系统**复现**。属异常残留。
- **待拍板**：删除 `Claw/`（破坏性，需造物主确认）。

## 四、P2 — 架构提示（非硬违规）

### P2-1 README 部署章 GitHub 字样（占位）
- `README.md` L186-215：第十节"部署（GitHub Pages + 自有域名）"含 `github.com/<你的用户名>`、`*.github.io` 占位。
- 契约 06 第十节本就允许"部署教程"章节，且用占位符非真实坐标 → **边界合规**。
- 但若要严格执行"上线前坐标扫描零命中"，建议将部署章改为"部署到任意静态托管 / 自有域名"，淡化 GitHub Pages 字样。

### P2-2 assets 内部文件随公开层部署
- `assets/.build/*.py`、`assets/xlsx/*.xlsx` 部署在公开层，URL 可直接访问（仅 `robots.txt` 软屏蔽，Disallow /assets/ + Allow css/js/images）。
- 属既定架构（MEMORY："assets/ = 世界可运行主体（xlsx+build+产物）"）。
- 提示：若未来严格执行"公开层仅公开内容"，需将 `.build/` 与 `xlsx/` 迁入 `.workbuddy/` 私有层或私有仓库。

### P2-3 统计/广告真实 ID 嵌入公开页
- GA4 `G-B880S4NQVK`、百度统计 `2f4df5057c929092e36a0d6357e35261` 嵌入 index/topics/pages/404 及 `assets/js/main.js`。
- 属境外阶段"留三件套"既定决策（MEMORY："境外留三件套，境内迁移时关 GA4/AdSense"）→ **合规**。
- 提示：迁境内服务器时应按决策关 GA4/AdSense。

## 五、合规通过项（已核实）

- ✅ **README 对照契约 06 校准清单 8 项全过**：纯手工流声明（L9）/ 零内部引用（无 `.workbuddy|contracts|xlsx|.py|merge|build_homeplus|统计授权条`）/ 目录树无 assets/xlsx·.build / 定位"综合全品类"（L5）/ 无已删功能 / 与现状一致 / 11 章齐全 / 239 行（200~300 量级）。
- ✅ **内部 `.md` 治理合规**：公开层仅 `README.md` 为权威手册；其余全部在 `.workbuddy/`（宪法/契约/memory/overviews/reports/backups），无裸放游离 md。
- ✅ **pages 子目录与 README 声明一致**：实际 9 页（about/changelog/contact/disclaimer/guide/overview/privacy/sitemap/submit）↔ README L43-51 完全对应。
- ✅ **robots.txt 正确隔离**：`Disallow: /assets/` + `Allow: /assets/css|js|images`，屏蔽 `.build` 与 `xlsx`。
- ✅ **首页 / 专题页无 github 坐标泄露**；已删功能"统计授权条"公开层 0 处（未复发）。

## 六、下一步（待造物主拍板）

1. **P0**：改写 10 处 GitHub Pages 字样 → 中性备案号占位（建议我起草统一替换文案，你确认后执行）。
2. **P1-1**：邮箱处理方案（保留 / 表单 / 占位）三选一。
3. **P1-2 / P1-3**：确认删除 `directory/`、`Claw/`（破坏性，需你明确授权）。
4. **P2-1**（可选）：是否淡化 README 部署章 GitHub 字样。
