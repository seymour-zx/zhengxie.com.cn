# W0 地基止血 · 交付报告（2026-08-29）

> 执行负责人：ProjectManagementExpert（关德豪）｜波次：重建路线图 W0（1/4）
> 用户拍板：W0 全授权一次做完；M-1=方案甲；波次严格 W0→W1→W2→W3；build 脚本改动先出方案再授权。

## 一句话结论

**部署校验从"纸面要求"变为可执行闸门；全站 4 个 P0 级缺陷清零；8 项文档漂移全部回改；14 页 OG/Twitter 覆盖从 3/14 → 14/14。**

## 完成清单

| 项 | 内容 | 验证 |
|---|---|---|
| M-1 | 新建 `assets/.build/verify_site.py`，覆盖 08-测试流程 §3 八项检查，一键执行 | `verify_site.py` 八项全绿，EXIT=0 |
| P0-1 | `directory/index.html` 资源路径 `../../`→`../`（CSS+Logo 不再 404） | 校验项 1 PASS |
| P0-2 | OG/Twitter/apple-touch-icon 批量补齐 11 页；404 豁免 canonical | 实测 **14/14** |
| P0-4 | submit 表单明示"提交仅存本地、不会送达运营方" | 页面可见红条提示 |
| N-2 | directory hub JSON-LD @type → `CollectionPage`（契约 §3.1） | 校验项 7 PASS |
| N-3 | check_links.py 数据源/报告路径修复（原路径不存在，此前无法运行） | `--limit 3` 跑通，报告写入 `reports/` |
| M-2~M-8 | 文档漂移 21 处精确回改（CONVENTIONS/02/07/05/README） | 残留扫描为空 |

## 校验结果（`python assets/.build/verify_site.py`）

```
红线失败：0 项检查 | 严重失败：0 项检查 | 已知豁免：3 条
结果：✓ 八项全部通过。EXIT=0
```

## 新增豁免（已登记整改清单，不阻断）

1. 裸 IP 外链 `202.106.125.196` —— 政务站仅提供 IP 入口
2. `http://www.cppcc.gov.cn` —— 未启用 https（实测 http 200 / https 000），待站点升级后回改
3. `http://www.cndca.org.cn` —— 境外网络不可达（实测双协议 000），待境内网络核验

## 回档信息

- 备份点：`.workbuddy/backups/2026-08-29-W0/`（40 文件 + MANIFEST.txt）
- 回档：按 MANIFEST.txt 说明复制回仓库根即可

## 未动事项（后续波次）

- **SEO-8b** sitemap lastmod 自动生成 → 方案已承诺，待授权（W2）
- **D-4** GA4/AdSense 去留 → 方案待出（W2）
- **M-6/C-3** `assets/xlsx/cards-pages/` 旧副本 → 移 trash 待确认（W3）
- C-1~C-6 其余清理项（W3）；B/C 组技术债与功能（W1/W3）

## Git 提醒

按 Git 全停令，agent 未执行任何 git 命令。**W0 全部变更文件需由你本地提交**（`.workbuddy/` 除外相关见 .gitignore 政策）。
