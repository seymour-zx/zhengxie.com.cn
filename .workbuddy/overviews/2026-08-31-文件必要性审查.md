# 文件必要性审查 · 2026-08-31

> 触发：用户要求检查 `__identity_backup_2026-08-31` 是否需保留，并复查其他文件存在必要性。
> 范围：公开层（随 GitHub Pages 部署）+ 内部治理层 `.workbuddy/`（不部署）。

---

## 一、关于 P0 裁定（前次审计的修正）

用户裁定：**"GitHub Pages" 平台名可以公开，禁止的是仓库名/用户名**（如 `github.com/<user>/...`、`*.github.io`、`<user>.noreply.github.com`）。

复核结果：公开层**不存在任何真实仓库坐标泄露**。
- README 的 4 处命中全是 `<用户名>`/`<仓库名>` 占位符（契约 06 允许的教程写法）。
- `topics/search/index.html` 两处命中是 `https://github.com/search` 与卡片 `href="https://github.com"`——把**公共 GitHub 站**列为第三方搜索引擎（与百度/知乎并列），非本项目坐标。
- 10 处"托管于 GitHub Pages"为平台名披露，按新裁定**合规**。

**→ 前次 P0 误判，现撤销（close）。**

---

## 二、__identity_backup_2026-08-31 判定

**位置**：`.workbuddy/__identity_backup_2026-08-31/`（内部治理层，不部署）
**内容**：`BOOTSTRAP.md`、`IDENTITY.md`、`SOUL.md`、`USER.md`

| 比对项 | 结果 |
|---|---|
| 与活体 IDENTITY/SOUL/USER 正文 | **逐字一致**（cmp 仅报 byte4/line1 = 行尾 CRLF↔LF 差异） |
| 时间戳 | 备份 mtime 09:02:16；活体 mtime 10:21:29-30（晚 ~1h19m，活体随后规范化重写） |
| 内容性质 | 全部为 WorkBuddy **默认空模板**（未定制填充） |
| BOOTSTRAP.md | 其自身末尾写明 "When You're Done — Delete this file"；活体层已按此删除，备份冗余保留 |

**判定：冗余快照，不含独有项目价值。建议删除。**

---

## 三、其他文件必要性矩阵

### A. 内部治理层 `.workbuddy/`（不随站点部署，仅影响治理整洁度/磁盘）

| 路径 | 内容 | 必要性 | 建议 |
|---|---|---|---|
| `__identity_backup_2026-08-31/` | 默认身份模板快照 + BOOTSTRAP | 冗余 | **删除** |
| `staging/` | 空目录（0 条目） | 无 | **删除** |
| `backups/` | 20 目录 / 2.1MB，8/25–8/28 回档快照 | 高（项目铁律重视"可回档"） | **保留**；可择期清理 8/28 前旧档（需拍板） |
| `artifacts/overview.md` | 单次生成产物 2.6KB | 低 | 可删/可留 |
| `reports/consent-bar-diag-20260831.md` | 声明条诊断报告 8KB | 中（排障史） | 保留 |
| `overviews/` 14 份 | 8/29–8/31 审计/收口报告 | 中高（治理史） | 保留 |
| `contracts/` 6 份 | 契约（权威源） | 极高 | 保留 |
| `memory/` | 日志/长期记忆/台账 | 极高 | 保留 |
| `宪法.md` | 宪法（权威源） | 极高 | 保留 |
| `IDENTITY/SOUL/USER.md`（活体） | 身份模板（系统管） | 系统需要 | 保留 |

### B. 公开层（随 GitHub Pages 部署）

| 路径 | 必要性 | 建议 |
|---|---|---|
| `directory/` | 旧世界废弃空壳（N-03，仅含空子目录） | **删除**（用户已批） |
| `Claw/` | 异常空目录 | **删除**（用户已批） |
| `ads.txt`（58B） | AdSense 授权卖家文件 | 保留 |
| `CNAME`（15B） | 自定义域名绑定 | 保留 |
| `404.html` / `favicon.ico` / `robots.txt` / `sitemap.xml` / `index.html` / `README.md` | 核心 | 保留 |
| `assets/`（xlsx/json/.build/css/js/images） | 构建输入+产物 | 保留 |
| `pages/` / `topics/` | 核心内容 | 保留 |

---

## 四、可执行删除清单（待拍板）

1. `.workbuddy/__identity_backup_2026-08-31/` — 冗余快照（建议删）
2. `.workbuddy/staging/` — 空目录（建议删）
3. `directory/` — 用户已批删
4. `Claw/` — 用户已批删

**可选（非本次）**：`backups/` 中 8/28 前的旧回档快照，待用户决定是否压缩/清理。

---

## 五、待用户拍板

- 是否确认删除 ①`__identity_backup_2026-08-31/` ②`staging/`？
- 是否一并执行已批的 ③`directory/` ④`Claw/`？
- 是否对 `backups/` 旧档做清理（仅建议，未授权）？

---

## 六、执行记录（2026-08-31 11:06，造物主授权）

造物主回：「确认删除 ①__identity_backup_2026-08-31/ ②staging/ 一并执行 ③directory/ ④Claw/ 对 backups/ 中 8/28 前的旧回档做清理」。

| 项 | 结果 |
|---|---|
| ① `.workbuddy/__identity_backup_2026-08-31/` | 已删 |
| ② `.workbuddy/staging/` | 已删（空目录） |
| ③ `directory/` | 已删（根目录已无） |
| ④ `Claw/` | 已删（根目录已无） |
| ⑤ `backups/` 8/28 前旧档 | 已清（`2026-08-27-*` 7 个全删；`2026-08-25-*` 与 `2026-08-27-channels` 执行前盘点已不在目录，终态一致仅剩 8/28） |

**终态**：`backups/` 仅余 `2026-08-28-*` 共 10 个回档；根目录无 `directory/Claw`；`.workbuddy/` 无 `staging/__identity_backup`。
**悬空引用**：仅 `.md` 治理文档提及 `directory/`（旧世界史实描述，非运行时路径）；`*.py`/HTML/JS/CSS 零引用 → 不影响构建与站点。
**git**：4 项主删目录 + 旧回档均未被跟踪，git 收口由造物主本地执行。
