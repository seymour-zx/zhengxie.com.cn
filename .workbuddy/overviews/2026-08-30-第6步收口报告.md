# miniworld · 第 6 步收口报告：骨架（定稿）

> 状态：**完成**（2026-08-30 16:53，造物主确认）｜编制：SeniorProjectManager
> 详细定义见 `contracts/05-骨架.md`（定稿）

---

## 一、目录树（新世界唯一布局）

```
miniworld/
├── README.md / .gitignore（已有）
├── index.html                首页 = 政协+民主党派（build 生成）
├── 404.html / CNAME / robots.txt / sitemap.xml / ads.txt / favicon
├── topics/                   专题导航 hub + gov/ + search/（+ 未来 renda/百家姓）
├── pages/                    9 页（about/contact/guide/overview/sitemap/submit/privacy/disclaimer/changelog）
├── assets/                   世界可运行本体
│   ├── css/style.css         继承 + 改造（T6/随机漫步/声明条）
│   ├── js/main.js            继承 + 五项改造
│   ├── images/               logo.svg + webp（继承）
│   ├── json/manifest.json    PWA（继承保留）
│   ├── xlsx/                 cards.unified.xlsx + pages.unified.xlsx（数据真值源）
│   └── .build/               build.py / build_homeplus.py / check_links.py / verify_site.py + reports/
└── .workbuddy/               私有层（治理/记忆/契约/运营记录）
```

## 二、核心原则（独立小世界愿景）

- **assets/ = 世界可运行本体**（数据+构建+产物自包含）
- **.workbuddy = 内部治理**（剥离时不留）
- 未来完善后：剥离 .workbuddy → 独立运行（改 xlsx + 跑 py）；或整个复制给其他造物主

## 三、main.js 五项改造

随机漫步新玩法（不跳转+sticky 切换） / 广告注入（首次必出+比例） / T6 小米滑动修复 / 声明条关闭逻辑 / 保留原有（筛选/收藏/暗色/搜索/滑动/展开）

## 四、第 7 步输入（迁数据 · 预告）

1. 建 assets/ 目录（xlsx/.build/css/js/images/json）
2. 复制继承文件（从 oldworld：css/js/images/manifest）
3. **463 卡迁移新 schema**：
   - dir_path 改名：`directory/gov` → `topics/gov`、`directory/engine` → `topics/search`、`/` 保留
   - 首页 337 卡 → 细分 cat_name（按层级：全国/省级/副省级/地级/县级 + 党派/工商联/机构）——**脚本自动分类（按 title 关键词）+ 造物主校验**
   - gov 59 卡 → cat_name 保留 + 人大卡归"人大"类
   - search 67 卡 → cat_name 保留
   - 补 is_ad 空；pages 拼装字段按页面配置

---

*第 6 步完 · 下一步第 7 步「迁数据」，由造物主确认后启动。*
