# miniworld · 第 4 步收口报告：构建（定稿）

> 状态：**完成**（2026-08-30 16:08，6 项全部造物主确认）｜编制：SeniorProjectManager
> 详细定义见 `contracts/03-构建.md`（定稿）

---

## 一、构建管线

```
改 xlsx/模板 → python build.py → build_homeplus 片段化拼装生成全部页面
            → verify_site 出 xlsx 报告 → 造物主审阅拍板 → 上线
（check_links 保留，后按需改造；collect_meta / sync_star_sprite 已砍）
```

## 二、片段化拼装引擎（D-16）

页面 = 骨架 + 片段，**全部 xlsx 驱动，py 无页面特判**：
- head 元信息（title/meta/OG/Twitter/JSON-LD/canonical）← title/description/keywords + dir_path 自动
- GA4 ← stat_ga4；百度统计 ← stat_baidu；广告 ← ad_slots；搜索框 ← search_box；导语 ← channel_intro_enabled+channel_intro；卡片区 ← dir_path

## 三、xlsx 读取健壮性

读第一 tab / 自动识别表头行 / 列乱序不影响 / 额外列安全（造物主自加备注列不崩）

## 四、随机漫步改造（做-5）

随机展示不跳转 + sticky 分类容器切换（隐藏分类标签、显示随机漫步条）+ 广告双策略（首次必出 + 每批比例，参数放 build 常量）

## 五、小米滑动修复（T6）

touch-action: pan-x / 简化 preventDefault / 超时兜底 / 三端实测（小米/微信/Chrome）

## 六、verify_site → xlsx 报告（D-10）

八项校验 → xlsx（页面/检查项/级别/问题/建议/造物主决定 + 汇总 sheet）→ **造物主拍板上线**（人治闸门）

## 七、广告位编号体系

旧世界已有 ①top(slot 5952548493) ②bottom(slot 4856101005)；新世界 ad_slots 编号 = py 广告位片段库索引（AD_SLOT_1/2/3…）；loader 只注入一次；新增广告位 = AdSense 后台建单元 → py 加片段 → xlsx 引用

## 八、第 5 步输入（定校验 · 预告）

**注意**：第 5 步「定校验」与第 4 步 verify_site 改造**有重叠**（verify_site 已定案 xlsx 报告 + 八项校验）——建议第 5 步聚焦**完整校验策略**：
- L1 静态校验（verify_site 八项）✅ 已定
- 校验的通过门槛 / 严重分级 / 豁免机制（旧世界红绿灯总表）
- 死链检测节奏（check_links：收录前后 / 每月全量）
- 上线清单（浏览器手工项：功能/视觉/无障碍/禁用 JS 降级）
- 三端实测（小米/微信/Chrome）

是否**合并进第 4 步**（第 5 步取消）还是**保留第 5 步专注校验策略**？——待造物主定。

---

*第 4 步完 · 下一步第 5 步「定校验」（或与第 4 步合并），由造物主确认。*
